---
phase: 03-corroborated-dakshina-ledger
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - code/artifacts/api-server/src/lib/confirm-card.ts
  - code/artifacts/api-server/src/routes/whatsapp.ts
  - tests/api/ledger.spec.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03]

must_haves:
  truths:
    - "Interactive post-ritual card templates contain gratitude copy and a dynamic UPI deep link"
    - "Buttons are wired with unique payload actions matching the ledger-claim:{ledgerId} and ledger-confirm:{ledgerId} grammar"
    - "The webhook routes interactive responses to the corresponding ledger state machine helper"
    - "Sender phone number lookup is performed to resolve and verify ownership boundaries"
    - "Double tap button interactions are handled as no-ops without throwing errors"
  artifacts:
    - path: code/artifacts/api-server/src/lib/confirm-card.ts
      provides: "buildPostRitualPurohitCard, buildPostRitualFamilyCard"
    - path: code/artifacts/api-server/src/routes/whatsapp.ts
      provides: "Webhook handlers routing ledger-claim and ledger-confirm button payloads"
    - path: tests/api/ledger.spec.ts
      provides: "E2E integration test suite for payment corroboration"
  key_links:
    - from: code/artifacts/api-server/src/routes/whatsapp.ts
      to: code/artifacts/api-server/src/lib/ledger.ts
      via: "claimLedgerEntry and confirmLedgerEntry calls"
      pattern: "claimLedgerEntry\\(|confirmLedgerEntry\\("
---

<objective>
Design and implement the Devanagari Hindi post-ritual dakshina cards for both Purohit and Yajman contacts, route button action payloads in the webhook, and write E2E integration specs.
This completes the core loop of logging and corroborating payments on WhatsApp.
The webhook must authenticate the replying sender's phone number against the database to confirm relationship boundaries.
Double taps must exit early as no-ops.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-corroborated-dakshina-ledger/03-CONTEXT.md
@.planning/phases/03-corroborated-dakshina-ledger/03-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Post-Ritual Card Builders</name>
  <files>code/artifacts/api-server/src/lib/confirm-card.ts</files>
  <action>
    Modify `code/artifacts/api-server/src/lib/confirm-card.ts` to export:
    1. `buildPostRitualPurohitCard(ledgerId: string, familyName: string, eventLabel: string, upiLink: string): { type: "interactive"; interactive: Record<string, unknown> }`:
       Returns a button-interactive card body in Hindi:
       `📿 यजमान: ${familyName}\nअनुष्ठान: ${eventLabel}\n\nदक्षिणा भुगतान लिंक: ${upiLink}\n\nदक्षिणा प्राप्त होने पर पुष्टि करें:`
       Button: `Dakshina received ✓` carrying action ID `ledger-claim:${ledgerId}`.
    2. `buildPostRitualFamilyCard(ledgerId: string, purohitName: string, eventLabel: string, upiLink: string): { type: "interactive"; interactive: Record<string, unknown> }`:
       Returns a button-interactive card body in Hindi:
       `📿 पुरोहित: ${purohitName}\nअनुष्ठान: ${eventLabel}\n\nदक्षिणा अर्पण लिंक: ${upiLink}\n\nदक्षिणा अर्पण की पुष्टि करें (अनुष्ठान संपन्न हुआ):`
       Button: `Confirm ✓` carrying action ID `ledger-confirm:${ledgerId}`.
  </action>
  <acceptance_criteria>
    - Card templates export successfully.
    - Buttons are mapped to `ledger-claim:${ledgerId}` and `ledger-confirm:${ledgerId}` exactly.
    - No compilation errors.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Post-ritual card templates written.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Webhook Button Interactive Routing</name>
  <files>code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/whatsapp.ts
  </read_first>
  <action>
    Modify the WhatsApp webhook POST route `/whatsapp/webhook` to handle the new action prefixes:
    1. Parse interactive button payloads: check for `ledger-claim:*` and `ledger-confirm:*`.
    2. Respond with an immediate HTTP 200 to Meta to meet performance SLAs.
    3. Asynchronously execute the database updates and send WhatsApp confirmation replies:
       - For `ledger-claim:{ledgerId}`: Look up the replying sender's phone number in `purohits`. Verify the record exists and `purohit.id === ledger.purohitId` (Nit-2). If verified, call `claimLedgerEntry(ledgerId, purohit.id)`. Send confirmation text: `"दक्षिणा प्राप्त हुई, बही खाता अपडेट कर दिया गया है।"`. If double-tap or ownership mismatch occurs, handle gracefully and send no duplicate messages.
       - For `ledger-confirm:{ledgerId}`: Look up the replying sender's phone number in `yajmans`. Verify the record exists and `yajman.id === ledger.yajmanId` (isolation). If verified, call `confirmLedgerEntry(ledgerId, yajman.id)`. Send confirmation text: `"पुष्टि के लिए धन्यवाद।"`.
  </action>
  <acceptance_criteria>
    - Action payloads parse correctly.
    - Webhook responds immediately with HTTP 200.
    - Ownership verification boundaries are checked against database records.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Webhook button routing wired.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Playwright E2E Spec</name>
  <files>tests/api/ledger.spec.ts</files>
  <read_first>
    - tests/api/onboarding.spec.ts
  </read_first>
  <action>
    Create `tests/api/ledger.spec.ts` containing the Playwright E2E corroboration specs:
    1. Seed a mock purohit record and an associated yajman family record.
    2. Insert a ledger entry with status `pending`.
    3. Send a simulated webhook callback payload for `ledger-claim:${ledgerId}` from the purohit's registered phone number. Verify that the HTTP status returns 200 immediately, and the database status updates to `claimed`.
    4. Send a simulated webhook callback payload for `ledger-confirm:${ledgerId}` from the yajman's registered phone number. Verify that the HTTP status returns 200 immediately, and the database status updates to `corroborated`.
    5. Send a simulated webhook callback payload for a different phone number. Assert that the database status does not update (ownership boundary validation check).
  </action>
  <acceptance_criteria>
    - E2E spec runs and successfully validates status changes and security constraints.
    - `DATABASE_URL="..." INTERNAL_API_KEY="..." npx playwright test ledger` passes green.
  </acceptance_criteria>
  <verify>
    <automated>DB_PASS=$(awk '/### smaran-dev/{f=1} f && /db pass/{print $NF; exit}' docs/db-creds) && DATABASE_URL="postgresql://postgres.ajzoxgjvtxhzzkgxxmxv:${DB_PASS}@aws-1-ap-south-1.pooler.supabase.com:6543/postgres" INTERNAL_API_KEY="e2e-local-key" npx playwright test ledger</automated>
  </verify>
  <done>Playwright E2E specs verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Spoofing | Webhook claims/confirmations | mitigate | Parse incoming sender phone number (`msg.from`) and verify ownership boundaries relative to ledger records before invoking database state-machine updates |
| T-03-05 | Repudiation | Double confirms | mitigate | State machine exit paths return silently on double confirm requests, leaving status unchanged |
</threat_model>

<verification>
- E2E tests run successfully.
</verification>

<success_criteria>
- Post-ritual cards display correctly.
- Webhooks parse callback payloads and update status securely.
</success_criteria>

<output>
After completion, create `.planning/phases/03-corroborated-dakshina-ledger/03-02-SUMMARY.md`
</output>
