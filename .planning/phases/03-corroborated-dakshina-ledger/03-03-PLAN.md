---
phase: 03-corroborated-dakshina-ledger
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - code/lib/api-spec/openapi.yaml
  - code/artifacts/api-server/src/routes/ledger.ts
  - code/artifacts/api-server/src/routes/index.ts
  - tests/api/ledger.spec.ts
autonomous: true
requirements: [PAY-03]

must_haves:
  truths:
    - "GET /ledger/:id is gated by X-Internal-Key header verification matching INTERNAL_API_KEY"
    - "The serialized response maps exactly to the OpenAPI schema parameters"
    - "All database access in ledger.ts uses dynamic gating"
  artifacts:
    - path: code/lib/api-spec/openapi.yaml
      provides: "ledger tag + GET /ledger/{id} path configuration"
    - path: code/artifacts/api-server/src/routes/ledger.ts
      provides: "GET /ledger/:id route implementation"
---

<objective>
Expose an ops-inspection endpoint for ledger status queries (`GET /ledger/:id`), secure it via `X-Internal-Key` headers, and compile schemas with zod codegen.
This endpoint enables operations tools to track payment state without exposing sensitive PII data or gotra values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-corroborated-dakshina-ledger/03-CONTEXT.md
@.planning/phases/03-corroborated-dakshina-ledger/03-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend openapi.yaml with ledger tag and paths</name>
  <files>code/lib/api-spec/openapi.yaml</files>
  <action>
    Modify `code/lib/api-spec/openapi.yaml`.
    1. Append `ledger` to the tags block:
       ```yaml
       - name: ledger
         description: Ledger and Payment Corroboration Operations
       ```
    2. Add path `/ledger/{id}`:
       - Tag: `ledger`
       - OperationId: `getLedgerEntry`
       - Parameter: `id` (path, string, required, format: uuid)
       - Parameter: `X-Internal-Key` (header, string, required)
       - Response 200: Schema `LedgerStatus` returning `id`, `purohitId`, `yajmanId`, `eventId`, `amountCollected` (string/number), `paymentStatus` (enum: pending, claimed, corroborated), `purohitClaimedAt`, `familyConfirmedAt`, `localityKey`, `createdAt`.
       - Response 401: `ApiErrorMessage`
       - Response 404: `ApiErrorMessage`
    3. Run schema codegen:
       `pnpm --dir code --filter @workspace/api-spec run codegen`
  </action>
  <acceptance_criteria>
    - `openapi.yaml` parsed and generated.
    - Zod schemas compiled successfully.
    - `pnpm --dir code run typecheck:libs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-spec run codegen</automated>
  </verify>
  <done>OpenAPI spec for ledger endpoint completed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement routes/ledger.ts router</name>
  <files>code/artifacts/api-server/src/routes/ledger.ts, code/artifacts/api-server/src/routes/index.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/ingest-jobs.ts
  </read_first>
  <action>
    Create `code/artifacts/api-server/src/routes/ledger.ts`.
    1. Check `X-Internal-Key` against `process.env.INTERNAL_API_KEY` (reject with 401 if missing or wrong).
    2. Query `ledgerTable` by `id` using dynamic imports of `@workspace/db`. If not found, respond 404.
    3. Serialize the database record parameters using the generated `LedgerStatus.parse(...)` validation scheme before responding with 200.
    4. Register the router in `code/artifacts/api-server/src/routes/index.ts`.
  </action>
  <acceptance_criteria>
    - `ledgerRouter` registered in index.ts.
    - Header validation checks matches other routes.
    - `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>ledgerRouter registered and fully typechecking.</done>
</task>

<task type="auto">
  <name>Task 3: Add E2E tests for inspection route</name>
  <files>tests/api/ledger.spec.ts</files>
  <action>
    Extend `tests/api/ledger.spec.ts` with tests:
    1. Querying `GET /ledger/{id}` without header, asserting status is 401.
    2. Querying `GET /ledger/{id}` with header, asserting status is 200 and schema params match.
  </action>
  <acceptance_criteria>
    - Playwright test verified green: `DATABASE_URL="..." INTERNAL_API_KEY="..." npx playwright test ledger` passes.
  </acceptance_criteria>
  <verify>
    <automated>DB_PASS=$(awk '/### smaran-dev/{f=1} f && /db pass/{print $NF; exit}' docs/db-creds) && DATABASE_URL="postgresql://postgres.ajzoxgjvtxhzzkgxxmxv:${DB_PASS}@aws-1-ap-south-1.pooler.supabase.com:6543/postgres" INTERNAL_API_KEY="e2e-local-key" npx playwright test ledger</automated>
  </verify>
  <done>E2E tests for inspection endpoint verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | GET /ledger/:id unauthorized access | mitigate | Gated strictly behind X-Internal-Key header checking matching INTERNAL_API_KEY |
</threat_model>

<verification>
- Typechecking passes cleanly.
</verification>

<success_criteria>
- Inspection route returns correct schema metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-corroborated-dakshina-ledger/03-03-SUMMARY.md`
</output>
