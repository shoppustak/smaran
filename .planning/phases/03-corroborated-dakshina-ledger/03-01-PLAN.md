---
phase: 03-corroborated-dakshina-ledger
plan: 01
type: execute
wave: 1
depends_on: ["02-01", "02-08"]
files_modified:
  - code/artifacts/api-server/src/lib/upi.ts
  - code/artifacts/api-server/src/lib/ledger.ts
autonomous: true
requirements: [PAY-01, PAY-03]

must_haves:
  truths:
    - "UPI deep links are generated dynamically using the standard upi://pay URI scheme, including pa, pn, am, tn, and cu parameters"
    - "Ledger table states are strictly managed through the progression: pending -> claimed -> corroborated"
    - "Transitions require both claimant (purohitClaimedAt) and confirmation (familyConfirmedAt) timestamps"
    - "No single side's assertion can move a ledger row to corroborated"
    - "All database access in ledger.ts uses dynamic gating to avoid throwing if DATABASE_URL is unset"
  artifacts:
    - path: code/artifacts/api-server/src/lib/upi.ts
      provides: "buildUpiDeepLink"
    - path: code/artifacts/api-server/src/lib/ledger.ts
      provides: "createLedgerEntry, claimLedgerEntry, confirmLedgerEntry"
  key_links:
    - from: code/artifacts/api-server/src/lib/ledger.ts
      to: code/artifacts/api-server/src/lib/upi.ts
      via: "buildUpiDeepLink invocation"
      pattern: "buildUpiDeepLink\\("
---

<objective>
Implement the primary ledger state transition machine in `ledger.ts` and the dynamic UPI deep-link builder in `upi.ts`.
This satisfies the core business rule: dakshina is recorded ONLY when both parties have corroborated it.
The state machine strictly enforces that only the `pending` state can transition to `claimed`, and only the `claimed` state can transition to `corroborated`.
Any attempt to violate these state transitions or override caller ownership boundaries must throw structured, catchable errors.
All DB calls are dynamically imported to ensure clean sandbox compilation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-corroborated-dakshina-ledger/03-CONTEXT.md
@.planning/phases/03-corroborated-dakshina-ledger/03-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend upi.ts with buildUpiDeepLink</name>
  <files>code/artifacts/api-server/src/lib/upi.ts</files>
  <action>
    Modify `code/artifacts/api-server/src/lib/upi.ts` to export:
    ```typescript
    export function buildUpiDeepLink(
      vpa: string,
      payeeName: string,
      amount: number,
      transactionNote: string
    ): string {
      const params = new URLSearchParams({
        pa: vpa,
        pn: payeeName,
        am: amount.toFixed(2),
        tn: transactionNote,
        cu: "INR",
      });
      return `upi://pay?${params.toString()}`;
    }
    ```
    Ensure it handles parameter URL-encoding correctly.
  </action>
  <acceptance_criteria>
    - `buildUpiDeepLink` is exported.
    - Generated links match the format `upi://pay?pa=...&pn=...&am=...&tn=...&cu=INR`.
    - Typecheck compiles cleanly: `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>buildUpiDeepLink helper added to upi.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Create ledger.ts state machine</name>
  <files>code/artifacts/api-server/src/lib/ledger.ts</files>
  <read_first>
    - code/lib/db/src/schema/ledger.ts
  </read_first>
  <action>
    Create `code/artifacts/api-server/src/lib/ledger.ts`.
    1. Define custom error classes:
       - `LedgerDbUnavailableError`: database connection string unset.
       - `LedgerNotFoundError`: ledger record by ID not found.
       - `LedgerOwnershipError`: caller is not authorized for the record.
       - `LedgerStateTransitionError`: invalid state-machine transition requested.
    2. Implement the following functions (using dynamic imports of `@workspace/db`):
       - `createLedgerEntry(purohitId: string, yajmanId: string, eventId: string | null, amount: number | null, localityKey: string): Promise<Ledger>`:
         Inserts a new row into `ledgerTable` with `paymentStatus: "pending"`.
       - `claimLedgerEntry(ledgerId: string, claimingPurohitId: string): Promise<void>`:
         Queries the ledger entry. Asserts existence. Asserts `ledger.purohitId === claimingPurohitId` (throws `LedgerOwnershipError` on mismatch). Asserts `ledger.paymentStatus === "pending"` (throws `LedgerStateTransitionError` on mismatch, except if status is already `claimed` or `corroborated` where it must exit silently to satisfy double-tap idempotency). Updates status to `claimed` and sets `purohitClaimedAt` to the current timestamp.
       - `confirmLedgerEntry(ledgerId: string, confirmingYajmanId: string): Promise<void>`:
         Queries the ledger entry. Asserts existence. Asserts `ledger.yajmanId === confirmingYajmanId` (throws `LedgerOwnershipError` on mismatch). Asserts `ledger.paymentStatus === "claimed"` (throws `LedgerStateTransitionError` on mismatch, except if status is already `corroborated` where it must exit silently to satisfy idempotency). Updates status to `corroborated` and sets `familyConfirmedAt` to the current timestamp.
  </action>
  <acceptance_criteria>
    - Functions are fully exported.
    - Ownership checks throw `LedgerOwnershipError` on mismatch.
    - Transitions throw `LedgerStateTransitionError` on regression (e.g. claimed -> pending).
    - Status changes are saved to PostgreSQL.
    - Sandbox-safe: does not throw on file load when `DATABASE_URL` is unset.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>ledger.ts state machine written and verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | claimLedgerEntry | mitigate | Assert `claimingPurohitId` matches `ledger.purohitId` before updating status, throwing `LedgerOwnershipError` on violation |
| T-03-02 | Elevation of Privilege | confirmLedgerEntry | mitigate | Assert `confirmingYajmanId` matches `ledger.yajmanId` before updating status, throwing `LedgerOwnershipError` on violation |
| T-03-03 | Tampering | paymentStatus updates | mitigate | Restrict status transitions to `pending -> claimed -> corroborated` sequence. Throw `LedgerStateTransitionError` if state is regressed. |
</threat_model>

<verification>
- Typechecking passes cleanly.
</verification>

<success_criteria>
- Dynamic UPI deep links are correctly formatted.
- Ledger states transition in correct order with ownership boundaries enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-corroborated-dakshina-ledger/03-01-SUMMARY.md`
</output>
