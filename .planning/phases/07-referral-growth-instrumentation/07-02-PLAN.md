---
phase: 07-referral-growth-instrumentation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - code/lib/api-spec/openapi.yaml
  - code/artifacts/api-server/src/routes/metrics.ts
  - code/artifacts/api-server/src/routes/index.ts
  - tests/api/referral.spec.ts
autonomous: true
requirements: [GROW-03]

must_haves:
  truths:
    - "GET /metrics/observed-k is gated by X-Internal-Key header checking matching INTERNAL_API_KEY"
    - "observed-k calculations group purohit signups into weekly cohorts and divide referred activations by active referrers"
    - "All database access in metrics.ts uses dynamic gating"
  artifacts:
    - path: code/lib/api-spec/openapi.yaml
      provides: "metrics schema + GET /metrics/observed-k path configuration"
    - path: code/artifacts/api-server/src/routes/metrics.ts
      provides: "GET /metrics/observed-k route implementation"
    - path: tests/api/referral.spec.ts
      provides: "E2E integration test suite validating referral mechanics and observed-k calculation"
---

<objective>
Expose the X-Internal-Key gated endpoint `/metrics/observed-k` returning weekly cohort growth metrics, configure OpenAPI schemas with zod codegen, and write E2E integration specs.
This enables growth telemetry tracking under strict authentication.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-referral-growth-instrumentation/07-CONTEXT.md
@.planning/phases/07-referral-growth-instrumentation/07-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /metrics/observed-k to openapi.yaml and run codegen</name>
  <files>code/lib/api-spec/openapi.yaml</files>
  <action>
    Modify `code/lib/api-spec/openapi.yaml`.
    1. Append `metrics` to the tags block:
       ```yaml
       - name: metrics
         description: Growth Telemetry and Cohort Metrics
       ```
    2. Add path `/metrics/observed-k`:
       - Tag: `metrics`
       - OperationId: `getObservedK`
       - Parameter: `X-Internal-Key` (header, string, required)
       - Response 200: Schema `CohortMetricsList` returning an array of cohort objects, each containing:
         - `week` (string, week start date)
         - `cohortSize` (integer, signups count)
         - `referredActivations` (integer, referred signup activations count)
         - `observedK` (number, growth metric calculation)
       - Response 401: `ApiErrorMessage`
    3. Run schema codegen:
       `pnpm --dir code --filter @workspace/api-spec run codegen`
  </action>
  <acceptance_criteria>
    - `openapi.yaml` updated successfully.
    - Zod schemas compiled successfully.
    - `pnpm --dir code run typecheck:libs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-spec run codegen</automated>
  </verify>
  <done>OpenAPI spec for metrics completed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement routes/metrics.ts router</name>
  <files>code/artifacts/api-server/src/routes/metrics.ts, code/artifacts/api-server/src/routes/index.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/ingest-jobs.ts
  </read_first>
  <action>
    Create `code/artifacts/api-server/src/routes/metrics.ts`.
    1. Gating Check: Verify `X-Internal-Key` header matching `process.env.INTERNAL_API_KEY` (reject with 401 on mismatch).
    2. Query database using dynamic imports of `@workspace/db`:
       - Group all active `purohits` by their creation week (truncated to Monday).
       - Count the number of referred activations in each cohort (purohits in that cohort having a valid `referred_by_purohit_id` reference).
       - Calculate `observedK = referred_signups / cohort_size`.
    3. Return JSON payload matching the generated zod schema.
    4. Register the router in `code/artifacts/api-server/src/routes/index.ts`.
  </action>
  <acceptance_criteria>
    - `metricsRouter` registered in index.ts.
    - Header validation is enforced.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>metricsRouter implemented and registered.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Playwright E2E Spec</name>
  <files>tests/api/referral.spec.ts</files>
  <action>
    Create `tests/api/referral.spec.ts` containing Playwright integration tests:
    1. Seed a mock purohit.
    2. Request a referral card via `"referral"` text command and assert URL formats.
    3. Simulate a new onboarding conversation starting with the invite text payload. Complete onboarding.
    4. Assert that the new purohit record in Postgres has the correct referrer ID.
    5. Query `GET /metrics/observed-k` with headers and assert the calculated cohort metrics match the invite conversions.
    6. Verify that requests without headers fail with 401.
  </action>
  <acceptance_criteria>
    - Specs run and successfully verify referral link tracking and cohort calculations.
    - `DATABASE_URL="..." INTERNAL_API_KEY="..." npx playwright test referral` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>DB_PASS=$(awk '/### smaran-dev/{f=1} f && /db pass/{print $NF; exit}' docs/db-creds) && DATABASE_URL="postgresql://postgres.ajzoxgjvtxhzzkgxxmxv:${DB_PASS}@aws-1-ap-south-1.pooler.supabase.com:6543/postgres" INTERNAL_API_KEY="e2e-local-key" npx playwright test referral</automated>
  </verify>
  <done>Playwright E2E spec verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | GET /metrics/observed-k unauthorized access | mitigate | Restrict metrics retrieval behind X-Internal-Key checks matching INTERNAL_API_KEY |
</threat_model>

<verification>
- Playwright E2E tests run successfully.
</verification>

<success_criteria>
- Cohort metrics accurately calculate observed-k.
- Access gating is fully verified.
</success_criteria>

<output>
After completion, create `.planning/phases/07-referral-growth-instrumentation/07-02-SUMMARY.md`
</output>
