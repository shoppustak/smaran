---
phase: 07-referral-growth-instrumentation
plan: 01
type: execute
wave: 1
depends_on: ["06-02"]
files_modified:
  - code/artifacts/api-server/src/lib/confirm-card.ts
  - code/artifacts/api-server/src/routes/whatsapp.ts
autonomous: true
requirements: [GROW-01, GROW-02]

must_haves:
  truths:
    - "A purohit can request a referral card using text command 'referral' or 'आमंत्रण'"
    - "The referral card returns a deep link url pointing to the bot with referrer tag invite:{purohitId}"
    - "First-contact onboarding messages parsing invite:{referrerId} populate the referred_by_purohit_id field"
    - "Referrer UUIDs are validated to exist in the database before referencing"
  artifacts:
    - path: code/artifacts/api-server/src/lib/confirm-card.ts
      provides: "buildReferralCard"
---

<objective>
Implement referral card interactive builders and configure webhook onboarding message routers to parse, validate, and persist referrer references in the database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-referral-growth-instrumentation/07-CONTEXT.md
@.planning/phases/07-referral-growth-instrumentation/07-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Referral Card Message Template</name>
  <files>code/artifacts/api-server/src/lib/confirm-card.ts</files>
  <action>
    Modify `code/artifacts/api-server/src/lib/confirm-card.ts` to export:
    `buildReferralCard(purohitId: string, purohitName: string): { type: "interactive"; interactive: Record<string, unknown> }`.
    It constructs a button-interactive card:
    - Body:
      `🚩 पुरोहित आमंत्रण कार्ड\n\nअपने साथी पुरोहित-जी को स्मरण से जोड़ें। वे इस लिंक पर क्लिक करके सीधे ऑनबोर्ड हो सकते हैं:`
    - Button: `आमंत्रण भेजें` containing the pre-filled message URL:
      `https://wa.me/${process.env.WHATSAPP_BOT_NUMBER || "12345"}?text=invite:${purohitId}`
  </action>
  <acceptance_criteria>
    - Card builder exports correctly.
    - Deep-link contains the correct `invite:{referrer_id}` syntax.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Referral card template written.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Referral Command and Invite Link Webhook Parsing</name>
  <files>code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/whatsapp.ts
  </read_first>
  <action>
    Modify the WhatsApp webhook POST route `/whatsapp/webhook`:
    1. Parse incoming text commands `"referral"` or `"आमंत्रण"`. Return the referral card via `buildReferralCard` to the purohit.
    2. Intercept incoming text messages starting with `invite:`. Extract the referrer ID (UUID).
    3. Check the database `purohits` table to assert that the referrer ID exists.
    4. If the referrer is valid, store the referrer UUID in the onboarding state.
    5. Save the referrer UUID into the `referred_by_purohit_id` column during onboarding completion.
  </action>
  <acceptance_criteria>
    - Webhook responds to referral commands.
    - Webhook parses and validates `invite:*` inputs on onboarding.
    - Persists valid referrals to the database.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Webhook referral parsing and routing completed.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | Invalid referrer UUIDs | mitigate | Validate that parsed referrer UUIDs exist in the purohits table before saving references |
</threat_model>

<verification>
- Typechecking passes cleanly.
</verification>

<success_criteria>
- Referral links generate and parse correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-referral-growth-instrumentation/07-01-SUMMARY.md`
</output>
