---
phase: 01-platform-foundation-purohit-onboarding
plan: 05
type: execute
wave: 4
depends_on: ["04"]
files_modified: []
autonomous: false
requirements: [ONBD-01]
user_setup:
  - service: meta-whatsapp
    why: "Meta's 2026 general-purpose-AI prohibition requires WhatsApp bots to be registered as a task-specific agent before operating at scale -- ROADMAP.md's Phase 1 success criterion #4 (per .planning/intel/constraints.md's Platform & API Contracts section). This is a Meta Business Manager declaration with no public CLI/API Claude can drive on the user's behalf."
    dashboard_config:
      - task: "Declare the WhatsApp app's task-specific purpose"
        location: "Meta Business Manager -> WhatsApp Business Platform -> App/Business verification, for the app tied to the smaran-dev test number"

must_haves:
  truths:
    - "The bot's narrow task-specific purpose is declared to Meta for the WhatsApp app used by Phase 1, closing ROADMAP.md's Phase 1 success criterion #4"
    - "D-01: only the task-specific-agent declaration is in scope here -- Utility-template submission and Meta's per-template review remain deferred to Phase 4, per CONTEXT.md D-01"
  artifacts: []
  key_links: []
---

<objective>
Close ROADMAP.md's Phase 1 success criterion #4 ("The bot is registered with Meta as a
task-specific agent") -- an account-level Meta Business Manager compliance declaration, not a code
artifact. This is scoped narrowly per D-01: CONTEXT.md's D-01 explicitly defers Utility-template
submission and Meta's per-template review process to Phase 4 ("No Utility template submission,
Meta review, or template-send code path is built in Phase 1"), since Phase 1's onboarding replies
are 100% free-form session-bound text (no template needed). D-01 does NOT address the separate,
narrower task-specific-agent purpose declaration -- that is a one-time account/app-level setting,
distinct from per-template content review, and is required for the bot to operate under Meta's
policy at all, independent of whether templates are used yet.

Purpose: Make this phase's 4th ROADMAP success criterion reachable without silently dropping it,
while being explicit that the template-submission portion of that same criterion sentence is
correctly out of scope per D-01.
Output: A confirmed (or explicitly blocked) Meta task-specific-agent declaration for the WhatsApp
app used by this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/intel/constraints.md
@.planning/phases/01-platform-foundation-purohit-onboarding/01-CONTEXT.md
@knowledgebase/01-Architecture/smaran-platform-architecture.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Register Smaran as a Meta task-specific WhatsApp agent</name>
  <files>none -- Meta Business Manager dashboard configuration, no repo files</files>
  <read_first>
    .planning/intel/constraints.md section "Platform & API Contracts" (exact task-specific-purpose phrase to declare)
    .planning/phases/01-platform-foundation-purohit-onboarding/01-CONTEXT.md D-01 (confirms only the template-review path is deferred, not this declaration)
    knowledgebase/01-Architecture/smaran-platform-architecture.md section Deployment (which app/number is in use)
  </read_first>
  <action>
    Sign in to Meta Business Manager for the WhatsApp Business app currently used for the
    smaran-dev test number. Navigate to WhatsApp Business Platform settings -> Business/App
    verification, and declare the app's specific task using the exact phrase already used in
    this project's own source docs: "ritual calendar and ledger assistant for Hindu priests"
    (from .planning/intel/constraints.md, section "Platform & API Contracts"). Submit for review
    if Meta's flow requires it. The production Meta webhook is separately still unregistered,
    pending business verification, and is explicitly out of scope for this task -- only the
    smaran-dev test app's declaration is in scope here.
  </action>
  <what-built>
    Nothing code-side -- this is a Meta Business Manager compliance declaration, not a build
    artifact. Every other artifact in this phase (schema, onboarding flow, wiring, E2E test) is
    already complete and independently functional against the smaran-dev test app per D-01 (no
    template or Meta-review code path was needed to make onboarding work). This task exists only
    to close the account-level registration criterion named explicitly in ROADMAP.md's Phase 1
    success criteria.
  </what-built>
  <how-to-verify>
    1. Sign in to Meta Business Manager for the WhatsApp Business app currently used for the
       smaran-dev test number (see knowledgebase/01-Architecture/smaran-platform-architecture.md
       section Deployment for which app/project is in use -- the production Meta webhook is
       separately still unregistered, pending business verification, and is explicitly out of
       scope for this task).
    2. Navigate to WhatsApp Business Platform settings -> Business/App verification, and declare
       the app's specific task using the exact phrase already used in this project's own source
       docs: "ritual calendar and ledger assistant for Hindu priests" (from
       .planning/intel/constraints.md, section "Platform & API Contracts").
    3. Submit for review if Meta's flow requires it, and note the resulting status
       (approved / pending / rejected / not-yet-available-for-test-apps).
  </how-to-verify>
  <verify>
    <automated>MISSING -- Meta Business Manager has no CLI/API this repo can drive; completion is proven only by the human-reported status captured via resume-signal below, recorded verbatim in this plan's SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The human operator has replied with one of: "registered", "pending review", "not applicable to test apps", or a stated blocker.
    - The reported status (and the exact phrase submitted, if any) is recorded verbatim in `01-05-SUMMARY.md`.
  </acceptance_criteria>
  <done>Registration attempted and status recorded; ROADMAP.md's Phase 1 success criterion #4 is either closed or explicitly tracked as blocked, never silently dropped.</done>
  <resume-signal>Reply with the registration status ("registered", "pending review", "not applicable to test apps", or describe any blocker) once you've completed or attempted this step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries or code are introduced by this plan -- it is a Meta dashboard
configuration action with zero repository changes. Threats relevant to Meta webhook trust (e.g.
T-01-01, unsigned webhook payloads) are already tracked in `01-04-PLAN.md`'s threat model and are
unaffected by this task's outcome.
</threat_model>

<verification>
- User-reported registration status recorded in this plan's SUMMARY.
</verification>

<success_criteria>
- ROADMAP.md's Phase 1 success criterion #4 (task-specific-agent registration clause) is either
  confirmed complete or explicitly tracked as blocked with a stated reason -- never silently
  dropped.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-purohit-onboarding/01-05-SUMMARY.md`
</output>
