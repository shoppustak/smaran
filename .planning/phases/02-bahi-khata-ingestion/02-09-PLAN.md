---
phase: 02-bahi-khata-ingestion
plan: 09
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - code/lib/api-spec/openapi.yaml
  - code/artifacts/api-server/src/routes/ingest-jobs.ts
  - code/artifacts/api-server/src/routes/index.ts
autonomous: true
requirements: [ING-03]

must_haves:
  truths:
    - "Raw transcript and extraction JSON are purged from ingest_jobs 30 days after a job reaches a terminal status, satisfying §8's retention table"
    - "An ops-inspection endpoint can check an ingest job's status without ever re-exposing its raw transcript/extraction content"
  artifacts:
    - path: code/artifacts/api-server/src/routes/ingest-jobs.ts
      provides: "GET /ingest-jobs/:id, POST /ingest-jobs/purge"
    - path: code/lib/api-spec/openapi.yaml
      provides: "ingestion tag + 2 new paths"
  key_links:
    - from: code/artifacts/api-server/src/routes/index.ts
      to: code/artifacts/api-server/src/routes/ingest-jobs.ts
      via: "router.use(ingestJobsRouter)"
      pattern: "router\\.use\\(ingestJobsRouter\\)"
---

<objective>
Add ops routes for `ingest_jobs`: a PII-minimal status-inspection GET endpoint, and a
`POST /ingest-jobs/purge` endpoint an external cron pinger hits daily to enforce
`docs/ref-state-2-plan.md` §8's retention table (raw transcript purged 30 days after a job reaches
a terminal status; extraction JSON purged on the same schedule).

Purpose: §8's retention policy is a DPDP-alignment promise, not optional cleanup — "this must be
true, because the landing page and the sabha pitch will say it is." This plan is where that promise
becomes enforced code, following this repo's existing "$0-infra, external pinger hits a plain
endpoint" posture (the same shape `/keepalive` already established) rather than introducing a new
job-scheduler dependency.
Output: 2 new OpenAPI paths + generated zod schemas, `routes/ingest-jobs.ts`, registered in
`routes/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bahi-khata-ingestion/02-CONTEXT.md
@docs/ref-state-2-plan.md

<interfaces>
`/keepalive` path pattern to repeat (openapi.yaml, exact shape for "external pinger hits a GET/POST,
deterministic 200"):
```yaml
  /keepalive:
    get:
      operationId: keepalive
      tags: [health]
      summary: Keep-warm endpoint for external uptime pingers
      responses:
        "200":
          description: Always returned, regardless of database state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeepaliveStatus"
```
`ApiErrorMessage` schema to reuse as-is:
```yaml
    ApiErrorMessage:
      type: object
      properties:
        error:
          type: string
      required: [error]
```
`routes/index.ts` full current file (registration pattern to extend):
```typescript
import { Router, type IRouter } from "express";
import healthRouter from "./health";
import keepaliveRouter from "./keepalive";
import panchangRouter from "./panchang";
import whatsappRouter from "./whatsapp";

const router: IRouter = Router();
router.use(healthRouter);
router.use(keepaliveRouter);
router.use(panchangRouter);
router.use(whatsappRouter);
export default router;
```
DB-gating pattern (`keepalive.ts` lines 15-31) — mandatory for `ingest-jobs.ts`:
```typescript
if (process.env.DATABASE_URL) {
  const { pool } = await import("@workspace/db");
  ...
}
```
§8 retention table (exact):
| Artifact | Kept | Then |
|---|---|---|
| Audio bytes | Until transcription succeeds | Deleted immediately — never persisted (already satisfied structurally by plan 02-05's media.ts) |
| Raw transcript | 30 days (debugging window) | Purged by scheduled job |
| Extraction JSON | Until confirm/reject + 30 days | Purged |
| Confirmed fields | Permanent (the product) | Deletion-on-request honors cascade |

Codegen step (mandatory after any openapi.yaml edit):
```bash
pnpm --dir code --filter @workspace/api-spec run codegen
```
Regenerates `code/lib/api-zod/src/generated/api.ts` — never hand-edit generated files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: openapi.yaml — ingestion tag + 2 new paths, run codegen</name>
  <files>code/lib/api-spec/openapi.yaml</files>
  <read_first>
    - code/lib/api-spec/openapi.yaml (full current file)
    - code/lib/api-spec/orval.config.ts (codegen pipeline, confirms output paths)
  </read_first>
  <action>
    Add `- name: ingestion` to the `tags:` block (currently has `health`, `panchang`, `whatsapp`). Add
    `GET /ingest-jobs/{id}` (operationId `getIngestJob`, tag `ingestion`, path param `id` string
    required, 200 response schema `IngestJobStatus` with properties `id: string`, `kind: string` enum
    `[voice, photo]`, `status: string` enum `[received, transcribed, extracted, awaiting_confirm,
    confirmed, rejected, failed]`, `fieldScores: object` nullable, `error: string` nullable,
    `createdAt: string`, `updatedAt: string` — deliberately OMIT `transcript`/`extraction` from this
    schema, since raw religious-personal-data content must never be re-exposed through an
    ops-inspection endpoint; 404 response reusing `ApiErrorMessage`). Add `POST /ingest-jobs/purge`
    (operationId `purgeIngestJobs`, tag `ingestion`, no request body, 200 response schema
    `PurgeResult` with `purgedTranscripts: integer`, `purgedExtractions: integer`,
    `deletedRows: integer`) — matches the `/keepalive`-style "external pinger hits a plain endpoint"
    shape shown in the interfaces block, no new job-scheduler dependency. Run
    `pnpm --dir code --filter @workspace/api-spec run codegen` to regenerate `code/lib/api-zod/src/generated/*`.
  </action>
  <acceptance_criteria>
    - `grep -c "getIngestJob\|purgeIngestJobs" code/lib/api-spec/openapi.yaml` = 2
    - `grep -c "name: ingestion" code/lib/api-spec/openapi.yaml` = 1
    - `grep -ic "transcript\|extraction:" code/lib/api-spec/openapi.yaml` shows no such property under `IngestJobStatus`'s schema block (manual check — the schema intentionally excludes raw content fields)
    - `pnpm --dir code run typecheck:libs` exits 0 (validates generated zod output compiles)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-spec run codegen</automated>
  </verify>
  <done>openapi.yaml has 2 new ingestion-tagged paths with a deliberately PII-minimal response shape; codegen regenerated successfully.</done>
</task>

<task type="auto">
  <name>Task 2: routes/ingest-jobs.ts — implement the 2 endpoints + retention purge logic, register route</name>
  <files>code/artifacts/api-server/src/routes/ingest-jobs.ts, code/artifacts/api-server/src/routes/index.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/keepalive.ts (DB-gating + always-responsive pattern, see interfaces block above)
    - code/artifacts/api-server/src/routes/index.ts (current registration list, see interfaces block above)
    - code/lib/db/src/schema/ingest-jobs.ts (plan 02-01 output — exact column names)
    - code/lib/api-zod's generated types for IngestJobStatus / PurgeResult (Task 1 output)
  </read_first>
  <action>
    Create `routes/ingest-jobs.ts`. Implement `GET /ingest-jobs/:id` — DB-gated per the mandatory
    pattern, look up the row by id, `.parse()` the response against the generated
    `GetIngestJobResponse` (or equivalent orval-generated name — check the actual generated export
    name after Task 1's codegen run) zod schema before sending, respond 404 with `ApiErrorMessage`
    shape if not found. Implement `POST /ingest-jobs/purge` implementing §8's exact retention rule:
    for rows where `status IN ('confirmed','rejected','failed')` AND
    `updated_at < now() - interval '30 days'`, run an `UPDATE` that sets `transcript = NULL` and
    `extraction = NULL` (null out the sensitive columns, do NOT delete the row — `id`/`status`/
    `created_at` remain useful debugging metadata even after content purge). Return counts of
    affected rows in the `PurgeResult` shape (`purgedTranscripts`, `purgedExtractions`,
    `deletedRows: 0` since this implementation nulls columns rather than deleting rows — document
    this design choice in a code comment referencing §8). This endpoint is meant to be hit daily by
    an external cron pinger (cron-job.org, matching `keepalive.ts`'s own documented posture) — no
    in-process scheduler is introduced. Register the new router in `routes/index.ts`: add
    `import ingestJobsRouter from "./ingest-jobs";` and `router.use(ingestJobsRouter);` following the
    exact existing pattern.
  </action>
  <acceptance_criteria>
    - `grep -c "ingestJobsRouter" code/artifacts/api-server/src/routes/index.ts` >= 2 (import + router.use)
    - `grep -c "30 days\|interval" code/artifacts/api-server/src/routes/ingest-jobs.ts` >= 1
    - `grep -c "process.env.DATABASE_URL" code/artifacts/api-server/src/routes/ingest-jobs.ts` >= 1
    - `grep -ic "SELECT \*\|transcript,\|extraction," code/artifacts/api-server/src/routes/ingest-jobs.ts` — manual check that the GET handler's response construction never includes raw transcript/extraction fields in the outgoing JSON
    - `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>routes/ingest-jobs.ts committed, registered in routes/index.ts, purge endpoint nulls transcript/extraction on 30-day-old terminal-status jobs without deleting rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| external cron pinger -> POST /ingest-jobs/purge | unauthenticated-by-convention endpoint, matching /keepalive's existing posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Information Disclosure | GET /ingest-jobs/:id response | mitigate | Response schema deliberately excludes transcript/extraction raw content — an ops-inspection endpoint should debug pipeline *status*, never re-expose the sensitive religious-personal-data content it's inspecting |
| T-02-18 | Denial of Service / Tampering | POST /ingest-jobs/purge, no auth | accept | Matches the existing /keepalive precedent (no auth on a $0-infra external-pinger endpoint) — worst case a repeated call is a harmless no-op once rows are already purged; consistent with this repo's existing $0-infra posture, not a new gap introduced here |
</threat_model>

<verification>
- `pnpm --dir code --filter @workspace/api-server run typecheck` passes
- `pnpm --dir code run typecheck:libs` passes after codegen
</verification>

<success_criteria>
- POST /ingest-jobs/purge nulls transcript/extraction on 30-day-old terminal jobs
- GET /ingest-jobs/:id never returns raw transcript/extraction content
</success_criteria>

<output>
After completion, create `.planning/phases/02-bahi-khata-ingestion/02-09-SUMMARY.md`
</output>
