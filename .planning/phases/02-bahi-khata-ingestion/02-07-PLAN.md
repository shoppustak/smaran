---
phase: 02-bahi-khata-ingestion
plan: 07
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified:
  - code/artifacts/api-server/src/lib/ingest.ts
autonomous: true
requirements: [ING-01, ING-02]

must_haves:
  truths:
    - "A purohit's voice note or photo triggers the full B->G pipeline ending in a confirm card, with every stage's status recorded in ingest_jobs"
    - "Audio over 5 minutes and any pipeline failure produce a friendly apology message and a failed status, never a silent drop or a crash"
    - "The confirm card renders canonical (fuzzy-matched) field values, not raw as-heard text, for fields at or above threshold — the same canonical values plan 02-08's confirmJob independently re-derives and writes, so display and write never diverge"
    - "A photo containing N families produces N ingest_jobs rows, each with its own extraction/fieldScores and its own confirm card and job id, sent sequentially — never one job holding a folded multi-family extraction (§10 fix, F-4)"
  artifacts:
    - path: code/artifacts/api-server/src/lib/ingest.ts
      provides: "createIngestJob, transitionJob, runIngestPipeline (photo path creates one additional sibling ingest_jobs row per extra detected family via createIngestJob, per §10)"
  key_links:
    - from: code/artifacts/api-server/src/lib/ingest.ts
      to: code/artifacts/api-server/src/lib/asr/index.ts
      via: "getAsrProvider()"
      pattern: "getAsrProvider\\("
    - from: code/artifacts/api-server/src/lib/ingest.ts
      to: code/artifacts/api-server/src/lib/confirm-card.ts
      via: "sendConfirmCard()"
      pattern: "sendConfirmCard\\("
---

<objective>
Build `ingest.ts`'s job-creation and pipeline-orchestration core: the `ingest_jobs` state machine
(`createIngestJob`/`transitionJob`) and `runIngestPipeline`, which wires Stages B through G
(media download -> ASR -> extraction/vision -> fuzzy-match -> confirm card) into one orchestrated
flow per `docs/ref-state-2-plan.md` §2-§6, including §10's photo-path delta: one `ingest_jobs` row
and one confirm card PER detected family, sent sequentially (F-4 fix).

Purpose: this is the "wire everything together" plan per Interface-First Task Ordering — every
adapter built in wave 1 (media, asr, extraction/vision, fuzzy-match, confirm-card) gets consumed
here for the first time. Stage H (the confirm-tap write) is deliberately NOT in this plan — it's
plan 02-08, so the write-gate logic stays isolated and independently auditable for ING-03.
Output: `ingest.ts` with the job state machine and full B->G orchestration, including the photo
path's N-family job split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bahi-khata-ingestion/02-CONTEXT.md
@docs/ref-state-2-plan.md

<interfaces>
DB-gating pattern to copy exactly (`keepalive.ts` lines 15-31, mandatory for every file touching `@workspace/db`):
```typescript
// @workspace/db throws at import time if DATABASE_URL is unset — the import is dynamic and
// gated behind an env check, never static.
if (process.env.DATABASE_URL) {
  const { db, pool } = await import("@workspace/db");
  ...
}
```
Unlike `keepalive.ts`'s graceful degradation, `ingest_jobs` is a hard write target — missing
`DATABASE_URL` should cause `createIngestJob` to log and return a typed failure the caller turns
into an apology-template send, never crash the process.

`ingest_jobs` status enum (plan 02-01 output, forward-only, per §3/§9):
`received -> transcribed -> extracted -> awaiting_confirm -> confirmed | rejected | failed`

Adapters this plan consumes (all wave-1 outputs — import ONLY these entry points, never their
internals, per each adapter's own adapter-boundary rule):
- `media.ts`: `downloadWhatsappMedia(mediaId): Promise<{ bytes: Buffer; mimeType: string }>`
- `asr/index.ts`: `getAsrProvider(): AsrProvider` (never import `asr/sarvam.ts`/`asr/openai.ts` directly)
- `extraction.ts`: `extractFields(transcript, caller): Promise<ExtractionResult>`
- `extraction-models/index.ts`: `getExtractionModelCaller(): ModelCaller` (never import the 3 concrete callers directly)
- `vision.ts`: `extractFieldsFromImage(imageBytes, caller): Promise<{ families: ExtractionResult[]; truncated: boolean }>`
- `fuzzy-match.ts`: `matchField(heard, vocab, maxEdits)` + `MAAS_MAX_EDITS`/`TITHI_MAX_EDITS`/`PAKSHA_MAX_EDITS`/`GOTRA_MAX_EDITS`
- `whatsapp-client.ts`: `sendWhatsappMessage(to, payload)`
- `confirm-card.ts`: `buildConfirmCard(...)`, `sendConfirmCard(to, card)`, `buildMultiFamilyFollowup()`

D-03 (locked): stale `awaiting_confirm` jobs get NO reminder nudge in this phase — that's a lapse-
recovery concern deferred to Phase 4 (BRAIN-02). Do not add any polling/scheduled-reminder logic.

D-04 (locked): zero-field extraction (garbled audio, unreadable photo) reuses the existing
`ingest_jobs.status = 'failed'` path plus a friendly re-record/re-photograph prompt — no new status
or retry-scheduling logic needed. This also covers the photo path's zero-family case (F-4: a photo
where vision detects zero families is the photo-path equivalent of a garbled/zero-field voice note).

D-05 (locked): `family_name` is NEVER routed through `matchField` — passed through as-heard.

§3 guard rails (exact): reject audio >5 min with "छोटे-छोटे notes भेजें — एक परिवार एक बार में";
max 3 retries on download failure (already implemented inside `media.ts`), then `failed` + apology.
§3 immediate ack: "सुन लिया, एक क्षण…" sent before any heavy processing.

§4/§12 Spike 1 note (Stage C, format gate): no in-repo transcode step exists (see plan 02-03's
Stage C note) — `runIngestPipeline` passes the downloaded bytes straight to
`getAsrProvider().transcribe(...)` unmodified; if Sarvam rejects the raw OGG/Opus format, that
surfaces as a normal `AsrError` and is handled by this plan's catch-all failure path below (transcode
sidecar remains a documented, contingent follow-up, not something this plan builds).

Display/write consistency note (resolves a prior review gap — see plan 02-08's interfaces block for
the write-side half): `buildConfirmCard` is called with a CANONICALIZED copy of the extraction, not
the raw as-heard object, for exactly the reason §6's own card example shows canonical Devanagari
forms rather than phonetic transcriptions. Plan 02-08's `confirmJob` independently re-runs the same
`matchField` calls against the same raw `extraction` at write time — because both plans apply
identical matchField+vocab+threshold logic to the same raw input, the values are guaranteed to
match without needing to persist or thread anything extra between the two plans.

Photo multi-family job-splitting design (F-4 fix — resolves a prior review gap in how §10's photo
path relates to the `ingest_jobs` one-row-per-extraction model): `vision.ts`'s
`extractFieldsFromImage` returns `{ families: ExtractionResult[]; truncated: boolean }`, one entry
per family detected on the page. Per §10 ("one confirm card per family, sent sequentially"), each
family becomes its OWN `ingest_jobs` row with its own extraction/fieldScores/confirm card/job id —
never one job holding a folded multi-family extraction. The webhook-created `job` argument (created
by plan 02-08's webhook handler and passed into `runIngestPipeline`) accounts for family index 0
only; this plan's `runIngestPipeline` creates `families.length - 1` ADDITIONAL sibling jobs via
`createIngestJob(purohit.id, "photo")` (the exact same function this plan's Task 1 defines — no new
job-creation entry point), one per remaining family, each starting fresh at `status: "received"` and
following the identical `extracted -> awaiting_confirm` transition sequence as the original job.
Confirm cards are sent ONE AT A TIME, awaited in family order (never `Promise.all`), matching §10's
"sent sequentially" requirement. Zero families (`families.length === 0`, an unreadable photo) reuses
D-04's zero-extraction failed-path on the ORIGINAL job only — no sibling jobs are created from
nothing. Plan 02-08's existing single-job `confirmJob`/`rejectJob` logic needs NO multi-family
awareness once this split is done correctly: each sibling job is a fully independent row, confirmed/
rejected by its own job id exactly like any other job.

Two DISTINCT multi-family messages exist in this pipeline — never conflate them: (1) VOICE path —
§5.1's extraction prompt returns exactly one family per note and sets `confidence_notes:
"multi-family"` when the purohit mentioned a second family in the same note; after that single
family's confirm card is sent, if `extraction.confidence_notes === "multi-family"`, send
`buildMultiFamilyFollowup()` ("दूसरे परिवार के लिए एक और voice note भेज दें 🙏") as a separate
message, per §6. (2) PHOTO path — §10's `truncated` flag means the page had MORE than 5 families and
was capped at 5; after all confirm cards for this burst are sent sequentially, if `truncated`, send a
plain text message with §10's own literal copy "अगला पन्ना भेजें" — this is NOT
`buildMultiFamilyFollowup()`, which is the voice-path string only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: createIngestJob + transitionJob — ingest_jobs state machine</name>
  <files>code/artifacts/api-server/src/lib/ingest.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/keepalive.ts (DB-gating pattern, see interfaces block above)
    - code/lib/db/src/schema/ingest-jobs.ts (plan 02-01 output — exact column names/types)
    - .planning/phases/01-platform-foundation-purohit-onboarding/01-PATTERNS.md (Phase 1's onboarding state-machine resumability philosophy, for the forward-only-transition pattern to mirror)
  </read_first>
  <action>
    Implement the mandatory DB-gating pattern shown in the interfaces block — `if
    (process.env.DATABASE_URL) { const { db, ingestJobs } = await import("@workspace/db"); ... }`,
    never a static top-level import. Implement `createIngestJob(purohitId: string, kind: "voice" |
    "photo"): Promise<IngestJob>` inserting a row with `status: "received"`, returning the created
    row (or throwing a typed `IngestDbUnavailableError` if `DATABASE_URL` is unset — never crash the
    process). This function is the SOLE `ingest_jobs`-row-creation entry point in the phase — later in
    this same plan (Task 2), the photo path calls it again, additional times, to create sibling jobs
    for extra detected families (F-4) — no second creation function is introduced for that case.
    Implement `transitionJob(jobId: string, next: IngestJobStatus, patch?: Partial<IngestJob>): Promise<void>`
    that reads the current row, asserts the transition only moves forward through the enum shown in
    the interfaces block (throw if a caller attempts to regress status), applies any `patch` fields
    (`transcript`/`extraction`/`fieldScores`/`error`), and always writes `updatedAt = now()` — the
    same forward-only resumability philosophy as Phase 1's onboarding state machine, but keyed by
    `ingest_job_id` (a new row per voice note/photo/family) rather than by phone number (a shared row).
  </action>
  <acceptance_criteria>
    - `grep -c "process.env.DATABASE_URL" code/artifacts/api-server/src/lib/ingest.ts` >= 1
    - `grep -c "await import(\"@workspace/db\")" code/artifacts/api-server/src/lib/ingest.ts` >= 1
    - `grep -c "^import.*@workspace/db" code/artifacts/api-server/src/lib/ingest.ts` = 0 (no static top-level import)
    - `grep -c "export.*createIngestJob" code/artifacts/api-server/src/lib/ingest.ts` = 1
    - `grep -c "export.*transitionJob" code/artifacts/api-server/src/lib/ingest.ts` = 1
    - `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>createIngestJob + transitionJob committed, forward-only status enforcement, DB-gated per repo convention.</done>
</task>

<task type="auto">
  <name>Task 2: runIngestPipeline — Stage B-G orchestration, including §10's N-family photo job split (F-4)</name>
  <files>code/artifacts/api-server/src/lib/ingest.ts</files>
  <read_first>
    - code/artifacts/api-server/src/lib/ingest.ts (Task 1 output, same file — extend it)
    - code/artifacts/api-server/src/lib/media.ts, asr/index.ts, extraction.ts, extraction-models/index.ts, vision.ts, fuzzy-match.ts, whatsapp-client.ts, confirm-card.ts (all wave-1 adapter entry points, see interfaces block above)
    - docs/ref-state-2-plan.md §2, §3, §5.3, §6, §10 (full pipeline shape, guard rails, fuzzy-match thresholds, photo-path delta)
  </read_first>
  <action>
    Implement `runIngestPipeline(job: IngestJob, purohit: { id: string; calendarSystem: string; localityKey: string; phoneNumber: string }, mediaId: string, audioDurationSeconds?: number): Promise<void>`
    orchestrating: (a) guard rail — if `job.kind === "voice"` and `audioDurationSeconds` exceeds 300,
    `transitionJob(job.id, "failed", { error: "audio_too_long" })` and send the §3 apology via
    `sendWhatsappMessage`, return early; (b) send the immediate ack ("सुन लिया, एक क्षण…") via
    `sendWhatsappMessage`; (c) `downloadWhatsappMedia(mediaId)` — on a thrown `MediaDownloadError`,
    `transitionJob(job.id, "failed", { error: String(err) })` + apology, return.

    Define a shared LOCAL helper (not exported — internal to `ingest.ts`), used by both branches
    below: `async function finalizeFamilyExtraction(familyJob: IngestJob, purohit: { id: string;
    calendarSystem: string; localityKey: string; phoneNumber: string }, extraction: ExtractionResult):
    Promise<void>` implementing steps (e)-(h) for ONE job/family: (e)
    `transitionJob(familyJob.id, "extracted", { extraction })` — persists the RAW `extraction`
    unchanged (per §9, `ingest_jobs.extraction` holds as-heard fields only); (f) for each field in
    `extraction` EXCEPT `family_name`, call `matchField` against the matching vocab table with that
    field's threshold constant from `fuzzy-match.ts` (`maas`->`MAAS_MAX_EDITS`,
    `tithi_name`->`TITHI_MAX_EDITS`, `paksha`->`PAKSHA_MAX_EDITS`, `gotra`->`GOTRA_MAX_EDITS`) —
    `family_name` is passed through as-heard, never through `matchField` (D-05) — accumulate a
    `fieldScores: Record<string, number>` map from each call's `matchScore`, AND build a
    `canonicalExtraction: ExtractionResult` — a shallow copy of `extraction` where each vocab-matched
    field is replaced by that SAME `matchField` call's returned `canonical` string (falling back to
    the as-heard value when `canonical` is `null`, the identical resolve-or-fallback rule plan 02-08's
    `confirmJob` independently re-applies at write time to the same raw `extraction`, so the confirm
    card and the eventual write are guaranteed to agree); `family_name` stays as-heard in
    `canonicalExtraction` (D-05); (g) `transitionJob(familyJob.id, "awaiting_confirm", { fieldScores })`
    — `canonicalExtraction` is a local, unpersisted rendering value only, never written to the DB; (h)
    `buildConfirmCard(canonicalExtraction, fieldScores, thresholds)` then
    `await sendConfirmCard(purohit.phoneNumber, card)` — the card renders the canonical (not raw
    as-heard) value for every field at/above threshold, matching §6's own example copy; a
    below-threshold field is still shown as `❓` by `buildConfirmCard`'s own gating regardless of which
    extraction object it's given.

    VOICE path (`job.kind === "voice"`): call `getAsrProvider().transcribe(bytes, { languageHint:
    "hi-IN" })`, `transitionJob(job.id, "transcribed", { transcript })`, `const extraction = await
    extractFields(transcript, getExtractionModelCaller())`, wrap the remainder in a try/catch that on
    any unexpected error calls `transitionJob(job.id, "failed", { error: String(err) })` plus the D-04
    apology, then inside the try: `await finalizeFamilyExtraction(job, purohit, extraction)`; after it
    resolves, if `extraction.confidence_notes === "multi-family"`, send
    `sendWhatsappMessage(purohit.phoneNumber, buildMultiFamilyFollowup())` as a separate message (§6's
    actual multi-family case — a second family mentioned in the same voice note).

    PHOTO path (`job.kind === "photo"`, F-4 fix — one `ingest_jobs` row per family, per §10 "one
    confirm card per family, sent sequentially"): wrap `extractFieldsFromImage(bytes,
    getExtractionModelCaller())` in a try/catch that on any unexpected error calls
    `transitionJob(job.id, "failed", { error: String(err) })` plus the D-04 apology and returns.
    Inside the try: `const { families, truncated } = await extractFieldsFromImage(...)`. If
    `families.length === 0` (unreadable photo, D-04's zero-extraction case), `transitionJob(job.id,
    "failed", { error: "no_families_detected" })` plus the D-04 apology, return — do not attempt to
    create sibling jobs from nothing. Otherwise, iterate `families` in order with a plain `for` loop
    (never `Promise.all` — confirm cards must send sequentially, per §10). For EACH family index `i`,
    wrap that single family's processing in its OWN inner try/catch so one family's failure never
    aborts the remaining families in the same photo burst: for `i === 0`, reuse the webhook-created
    `job` (no new row); for `i >= 1`, first call `const siblingJob = await createIngestJob(purohit.id,
    "photo")` (the SAME `createIngestJob` Task 1 defines), then in either case call `await
    finalizeFamilyExtraction(familyJobForThisIndex, purohit, families[i])`. On an error inside this
    inner try (job creation or `finalizeFamilyExtraction` failing for family `i`), transition that
    family's own job to `failed` with `error: String(err)` (fall back to the original `job.id` only if
    `createIngestJob` itself threw before a sibling row exists) and send one D-04 apology, then
    `continue` to the next family in the loop rather than aborting the whole burst. Await each
    iteration's inner try/catch fully before starting the next (sequential, not concurrent) so confirm
    cards arrive in family order. After the loop completes, if `truncated`, send a plain text message
    via `sendWhatsappMessage(purohit.phoneNumber, { type: "text", text: { body: "अगला पन्ना भेजें" } })`
    — §10's literal copy for a page capped at 5 families, distinct from `buildMultiFamilyFollowup()`
    (the voice-path string) — never conflate the two.

    Do not add any reminder/polling logic for stale `awaiting_confirm` jobs (D-03 — explicitly
    deferred to Phase 4).
  </action>
  <acceptance_criteria>
    - `grep -c "runIngestPipeline" code/artifacts/api-server/src/lib/ingest.ts` >= 1
    - `grep -c "getAsrProvider" code/artifacts/api-server/src/lib/ingest.ts` = 1
    - `grep -c "from \"\\./asr/sarvam\"\|from \"\\./asr/openai\"" code/artifacts/api-server/src/lib/ingest.ts` = 0
    - `grep -c "getExtractionModelCaller" code/artifacts/api-server/src/lib/ingest.ts` >= 1
    - `grep -c "from \"\\./extraction-models/claude\"\|from \"\\./extraction-models/gemini\"\|from \"\\./extraction-models/sarvam-m\"" code/artifacts/api-server/src/lib/ingest.ts` = 0
    - `grep -c "matchField(" code/artifacts/api-server/src/lib/ingest.ts` >= 1
    - `grep -c "canonicalExtraction" code/artifacts/api-server/src/lib/ingest.ts` >= 1 (proves the confirm card renders resolved canonical values, not raw as-heard text, for at/above-threshold fields — the display-side half of keeping the card and confirmJob's eventual write in agreement)
    - `grep -c "finalizeFamilyExtraction" code/artifacts/api-server/src/lib/ingest.ts` >= 3 (1 definition + at least 1 call site in the voice branch + at least 1 call site in the photo loop — proves both kinds route through the same shared per-family finalize logic, F-4)
    - `grep -c "createIngestJob(purohit.id, \"photo\")" code/artifacts/api-server/src/lib/ingest.ts` >= 1 (sibling-job creation call site inside the photo loop's `i >= 1` branch — proves F-4's job split actually creates additional rows, not just one job per photo)
    - `grep -c "no_families_detected" code/artifacts/api-server/src/lib/ingest.ts` >= 1 (D-04's zero-family failure path for an unreadable photo)
    - `grep -c "Promise.all" code/artifacts/api-server/src/lib/ingest.ts` = 0 (confirm cards must send sequentially per §10, never fanned out concurrently)
    - `grep -c "अगला पन्ना भेजें" code/artifacts/api-server/src/lib/ingest.ts` >= 1 (§10's literal truncated-page copy, distinct from buildMultiFamilyFollowup)
    - `grep -c "confidence_notes" code/artifacts/api-server/src/lib/ingest.ts` >= 1 (voice-path multi-family detection, distinct from the photo-path job split)
    - `grep -c "buildMultiFamilyFollowup(" code/artifacts/api-server/src/lib/ingest.ts` >= 1 (still used, now correctly scoped to the voice path's confidence_notes case only)
    - `grep -c "\"failed\"" code/artifacts/api-server/src/lib/ingest.ts` >= 3 (guard-rail path, voice/photo catch-all failure paths, and the zero-family/per-family inner-catch paths all transition to failed)
    - `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>runIngestPipeline committed, orchestrates B->G per §2, respects D-03 (no reminder-nudge), D-04 (failed-path reuse, including the photo zero-family case), D-05 (family_name never fuzzy-matched); never imports a provider SDK/adapter file directly outside the factory modules; the confirm card(s) it sends render canonicalized (fuzzy-matched) field values so the purohit sees the same value plan 02-08's confirmJob later writes; a photo containing N families produces N independent ingest_jobs rows and N confirm cards sent sequentially (never concurrently), each family's failure isolated from the others, per §10 (F-4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| purohit-controlled inbound media (mediaId, duration) -> ingest.ts orchestration -> 3 external AI providers in sequence | untrusted input drives real API spend and downstream writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Denial of Service | runIngestPipeline audio duration guard | mitigate | >5min audio rejected before any ASR/extraction API spend (§3) — prevents unbounded-cost AI calls from a single oversized note |
| T-02-15 | Information Disclosure | ingest_jobs.transcript/extraction (written here) | mitigate | Written to the columns established in plan 02-01, purged per §8's retention job (plan 02-09) — this plan populates the columns correctly so the purge job has a well-defined target; it does not itself implement deletion |
| T-02-16 | Elevation of Privilege | fuzzy-match threshold bypass | mitigate | `family_name` is structurally never routed through `matchField` in this file (enforced by acceptance_criteria and code review) — prevents accidental auto-correction of the one field D-05 says must never be forced to a canonical value |
| T-02-30 | Repudiation | photo path per-family job/card correspondence (F-4) | mitigate | Each detected family gets its own `ingest_jobs` row and its own confirm card/job id (never a folded multi-family job) — a purohit's ✓ tap on one family's card can never accidentally confirm a different family's data, since `confirmJob` (plan 02-08) resolves and writes only the single job id named in that card's `confirm:{jobId}` reply |
| T-02-31 | Denial of Service | one family's pipeline failure inside a multi-family photo burst (F-4) | mitigate | Each family's processing (sibling job creation + finalizeFamilyExtraction) runs inside its OWN inner try/catch — a failure on family 3 of 5 fails only that family's job and continues to families 4-5, rather than aborting the whole burst and silently dropping already-successful families' confirm cards |
</threat_model>

<verification>
- `pnpm --dir code --filter @workspace/api-server run typecheck` passes
- ingest.ts imports only factory entry points (asr/index.ts, extraction-models/index.ts), never concrete provider files
</verification>

<success_criteria>
- A voice/photo job runs end-to-end through B-G, recording every status transition
- Every failure mode (duration, download, ASR, extraction, zero-family) ends in status=failed + an apology message, never a crash
- The confirm card displays canonical field values matching what plan 02-08's confirmJob later writes
- A photo containing N families produces N ingest_jobs rows and N confirm cards, sent sequentially, per §10 — not one job with a folded extraction (F-4)
</success_criteria>

<output>
After completion, create `.planning/phases/02-bahi-khata-ingestion/02-07-SUMMARY.md`
</output>
