---
phase: 02-bahi-khata-ingestion
plan: 08
type: execute
wave: 3
depends_on: ["02-06", "02-07", "02-05", "02-01"]
files_modified:
  - code/artifacts/api-server/src/lib/ingest.ts
  - code/artifacts/api-server/src/routes/whatsapp.ts
  - tests/api/ingest.spec.ts
autonomous: true
requirements: [ING-01, ING-02, ING-03]

must_haves:
  truths:
    - "Tapping ✓ on a confirm card writes exactly one yajmans row (or reuses an explicitly-confirmed existing one) and one events row per extracted event, stamped with source/ingest_job_id/locality_key"
    - "Tapping ✏ or letting a job go stale never writes to yajmans/events"
    - "A double-tap of ✓ is a no-op, never a duplicate write"
    - "The webhook accepts audio/image/interactive message types with an immediate 200, matching the existing text-message contract"
  artifacts:
    - path: code/artifacts/api-server/src/lib/ingest.ts
      provides: "confirmJob/rejectJob — the phase's sole yajmans/events write gate"
    - path: code/artifacts/api-server/src/routes/whatsapp.ts
      provides: "audio/image/interactive routing into the ingest pipeline"
    - path: tests/api/ingest.spec.ts
      provides: "E2E webhook-acceptance coverage for the 3 new message types"
  key_links:
    - from: code/artifacts/api-server/src/routes/whatsapp.ts
      to: code/artifacts/api-server/src/lib/ingest.ts
      via: "confirmJob/rejectJob calls"
      pattern: "confirmJob\\(|rejectJob\\("
---

<objective>
Close the loop: implement Stage H's `confirmJob`/`rejectJob` (the ONLY code path in this entire
phase permitted to write to `yajmans`/`events`), wire the WhatsApp webhook to route
audio/image/interactive messages into the pipeline built in plans 02-05 through 02-07, and add E2E
coverage.

Purpose: this plan is the concrete, automatable proof of ING-03 ("nothing extracted reaches
yajmans/events without explicit purohit confirmation") and the phase-level invariant the threat
model requirement calls out by name — confirmation must be enforced, not merely trusted.
Output: `ingest.ts` extended with confirmJob/rejectJob, `whatsapp.ts` extended with 3 new message-
type routes, `tests/api/ingest.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bahi-khata-ingestion/02-CONTEXT.md
@docs/ref-state-2-plan.md

<interfaces>
§7 exact write/idempotency/provenance rules:
- Writes happen ONLY on ✓: upsert `yajmans` (dedupe on `purohit_id + normalized family_name` — on
  collision, ask "यह वही शर्मा परिवार है?" rather than silently merging), insert `events` rows.
- Provenance columns: `source: 'voice' | 'photo' | 'manual'`, `ingest_job_id`.
- Idempotency: confirm taps carry the job id; a double-tap is a no-op.
- Every write stamps `locality_key` from the purohit's record (density accounting).

Current `whatsapp.ts` webhook handler's message loop (lines 105-114, the only branch today):
```typescript
for (const msg of messages) {
  if (msg.type !== "text") continue;
  inboundMessages.push({ from: msg.from, text: msg.text?.body ?? "", receivedAt: new Date().toISOString() });
  if (inboundMessages.length > MAX_MESSAGES) inboundMessages.shift();
  req.log.info({ from: msg.from }, "Received WhatsApp message");
}
```
Critical constraint to preserve (whatsapp.ts, current lines 96-97): `res.sendStatus(200)` fires
BEFORE any processing — Meta retries aggressively on non-2xx. All new async ingestion logic (media
download, ASR, extraction, DB writes, confirm-card send) must happen after the early 200, never
awaited before the response.

Per §3, an audio message carries `msg.audio.id` (media ID), `msg.audio.voice: true`, and duration
information the caller uses for the 5-minute guard rail passed into `runIngestPipeline`. An image
message carries `msg.image.id`. An interactive reply carries `msg.interactive.button_reply.id` or
`msg.interactive.list_reply.id`.

Confirm-card reply id convention (this plan's own design, since §6 doesn't specify wire-format ids):
`confirm:{jobId}` for the ✓ tap, `edit:{jobId}` for the ✏ tap (opens the numbered correction list —
full field-edit flow beyond routing the initial tap is out of this plan's narrow scope; document any
remaining correction-flow work as a known follow-up in the SUMMARY), `reject:{jobId}` for an explicit
decline.

`ingest.ts` entry points this plan extends/consumes (plan 02-07 output, same file):
`createIngestJob(purohitId, kind)`, `transitionJob(jobId, status, patch)`, `runIngestPipeline(job, purohit, mediaId, audioDurationSeconds)`.

Existing E2E pattern to extend (`tests/api/whatsapp.spec.ts` lines 36-60):
```typescript
test("POST /whatsapp/webhook records an inbound text message, visible via GET /whatsapp/messages", async ({ request }) => {
  const webhookRes = await request.post("/api/whatsapp/webhook", {
    data: { entry: [{ changes: [{ value: { messages: [{ from, type: "text", text: { body: text } }] } }] }] },
  });
  expect(webhookRes.status()).toBe(200);
  ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: confirmJob/rejectJob — Stage H write logic (the ING-03 enforcement point)</name>
  <files>code/artifacts/api-server/src/lib/ingest.ts</files>
  <read_first>
    - code/artifacts/api-server/src/lib/ingest.ts (current state from plan 02-07 — extend it)
    - docs/ref-state-2-plan.md §7 (writes/idempotency/provenance, see interfaces block above)
    - code/lib/db/src/schema/yajmans.ts and events.ts (plan 02-01 output — exact column names)
  </read_first>
  <action>
    Implement `confirmJob(jobId: string): Promise<{ status: "written" | "duplicate-tap" | "collision-pending" }>`:
    read the job; if `status !== "awaiting_confirm"`, this is an idempotent no-op — return
    `{ status: "duplicate-tap" }` without touching `yajmans`/`events` at all (§7: "a double-tap is a
    no-op"). Otherwise: normalize `family_name` (trim, collapse whitespace, NFC-normalize — do NOT
    fuzzy-match it, per D-05), look up an existing `yajmans` row by `purohit_id + normalized
    family_name`. On a match, do NOT silently merge — send a WhatsApp interactive prompt
    ("यह वही शर्मा परिवार है?", reply ids `collision-yes:{jobId}` / `collision-no:{jobId}`) via
    `whatsapp-client.ts`'s `sendWhatsappMessage` and return `{ status: "collision-pending" }` without
    writing (document in the SUMMARY that resolving the collision reply itself reuses the same
    interactive-routing branch Task 2 adds — no new endpoint needed, just an additional reply-id
    prefix to handle as a narrow, explicitly-scoped follow-up). On no match: `INSERT INTO yajmans`
    with `source: job.kind` ('voice' | 'photo') and `locality_key` stamped from the purohit's record
    (§7's density-accounting requirement). Then `INSERT INTO events` one row per extracted event with
    `label`, `source: job.kind`, `ingestJobId: job.id`, and the CONFIRMED canonical `maas`/`paksha`/
    `tithi` values (the fuzzy-matched canonical strings from `job.fieldScores`'s matched output, not
    the raw as-heard strings). Finally `transitionJob(job.id, "confirmed")`. Implement
    `rejectJob(jobId: string): Promise<void>` — `transitionJob(job.id, "rejected")` only; this
    function must never call any yajmans/events insert. Add a comment at the top of `confirmJob`
    stating explicitly: "this is the only function in this phase's file set permitted to write to
    yajmans/events — do not add a second write path."
  </action>
  <acceptance_criteria>
    - `grep -c "export.*confirmJob" code/artifacts/api-server/src/lib/ingest.ts` = 1
    - `grep -c "export.*rejectJob" code/artifacts/api-server/src/lib/ingest.ts` = 1
    - `grep -c "duplicate-tap" code/artifacts/api-server/src/lib/ingest.ts` >= 1
    - `grep -rc "db.insert(yajmansTable\|db.insert(schema.yajmansTable\|\.insert(yajmansTable" code/artifacts/api-server/src/lib/*.ts code/artifacts/api-server/src/routes/*.ts` — total count across the entire phase's lib/routes files equals the count found only inside `confirmJob`'s function body (manual code-region verification, documented in SUMMARY) — this is the concrete, automatable proof of ING-03
    - within `rejectJob`'s function body specifically, `grep -A 10 "function rejectJob" code/artifacts/api-server/src/lib/ingest.ts | grep -c "insert(yajmansTable\|insert(eventsTable"` = 0
    - `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>confirmJob/rejectJob committed; confirmJob is the only code path in the entire phase that writes to yajmans/events; rejectJob writes nothing to those tables; double-tap is a documented no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook wiring — audio/image/interactive message routing</name>
  <files>code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/whatsapp.ts (current state after plan 02-05's helper extraction)
    - code/artifacts/api-server/src/lib/ingest.ts (Task 1 of this plan + plan 02-07 — createIngestJob, runIngestPipeline, confirmJob, rejectJob)
  </read_first>
  <action>
    Extend the existing `POST /whatsapp/webhook` handler's `for (const msg of messages)` loop (see
    interfaces block for the current text-only shape). Add a branch for `msg.type === "audio"`: read
    `msg.audio.id` and `msg.audio.voice`, look up the purohit by `msg.from` (phone number — reuse
    whatever purohit-lookup helper Phase 1's onboarding path already established; if none exists yet,
    query `purohits` by `phoneNumber` directly here), check for an existing `awaiting_confirm` job for
    this purohit — if found, call `rejectJob` on it first (re-record supersedes, per §6), then call
    `createIngestJob(purohit.id, "voice")` followed by `runIngestPipeline(job, purohit, msg.audio.id,
    msg.audio.duration)` as fire-and-forget (do not `await` before the response — the existing
    `res.sendStatus(200)` at the top of the handler must still fire first). Add a branch for
    `msg.type === "image"`: same shape with `msg.image.id`, `kind: "photo"`, no duration guard. Add a
    branch for `msg.type === "interactive"`: read `msg.interactive.button_reply?.id ??
    msg.interactive.list_reply?.id`, parse the `{action}:{jobId}` convention from the interfaces
    block, and route `confirm:*` to `confirmJob(jobId)`, `reject:*` to `rejectJob(jobId)` — log a
    warning and no-op for any unrecognized action prefix rather than throwing. None of this new logic
    runs before the existing early `res.sendStatus(200)` — preserve that exact ordering.
  </action>
  <acceptance_criteria>
    - `grep -c "msg.type === \"audio\"" code/artifacts/api-server/src/routes/whatsapp.ts` = 1
    - `grep -c "msg.type === \"image\"" code/artifacts/api-server/src/routes/whatsapp.ts` = 1
    - `grep -c "msg.type === \"interactive\"" code/artifacts/api-server/src/routes/whatsapp.ts` = 1
    - `grep -n "res.sendStatus(200)" code/artifacts/api-server/src/routes/whatsapp.ts` reports a line number strictly less than the line number of the new audio/image/interactive branches (manual ordering check, documented in SUMMARY)
    - `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>whatsapp.ts routes audio/image messages into the ingest pipeline and interactive button/list replies into confirmJob/rejectJob, without disturbing the existing text-message onboarding path or the early-200 constraint.</done>
</task>

<task type="auto">
  <name>Task 3: E2E test — tests/api/ingest.spec.ts</name>
  <files>tests/api/ingest.spec.ts</files>
  <read_first>
    - tests/api/whatsapp.spec.ts (pattern to extend, see interfaces block above)
    - playwright.config.ts (webServer env, baseURL — confirms no DATABASE_URL is set in the E2E environment)
  </read_first>
  <action>
    Add a Playwright spec mirroring `whatsapp.spec.ts`'s webhook-POST pattern with 3 tests: (1) POST
    the webhook envelope with `messages: [{ from, type: "audio", audio: { id: "test-media-id", voice: true, duration: 10 } }]`
    and assert `res.status() === 200` — this proves the webhook accepts and doesn't crash on the new
    message type; it does not assert successful transcription, since no real Sarvam/media credentials
    exist in the E2E environment. (2) Same shape for `messages: [{ from, type: "image", image: { id: "test-media-id" } }]`.
    (3) POST an `interactive` button-reply payload
    (`messages: [{ from, type: "interactive", interactive: { button_reply: { id: "confirm:nonexistent-job-id" } } }]`)
    and assert `res.status() === 200` — the webhook always 200s immediately; `confirmJob`'s lookup-
    miss/error path runs asynchronously after the response and must not throw synchronously into the
    handler. All 3 tests run without `DATABASE_URL` set, matching `whatsapp.spec.ts`'s existing E2E
    environment (per this phase's PATTERNS.md's documented caveat) — they only assert webhook-
    acceptance behavior, not DB state.
  </action>
  <acceptance_criteria>
    - `npx playwright test ingest` (repo root) exits 0 with 3 passing tests
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test ingest</automated>
  </verify>
  <done>tests/api/ingest.spec.ts committed and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| purohit's confirm-tap (WhatsApp interactive reply) -> ingest.ts confirmJob -> permanent yajmans/events tables | the single most important boundary in this entire phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Elevation of Privilege | confirmJob write gate | mitigate | confirmJob is the only function in the phase's entire file set permitted to insert into yajmans/events — enforced structurally (single call site) and verified by the grep-based acceptance_criteria in Task 1, directly satisfying ING-03 and this phase's threat-model requirement |
| T-02-22 | Repudiation | double-tap on an already-confirmed job | mitigate | confirmJob checks `status !== "awaiting_confirm"` before writing and returns a no-op for any status outside that window — a duplicate confirm tap (network retry, accidental double-tap) cannot create duplicate yajmans/events rows |
| T-02-23 | Tampering | family/gotra collision on confirm (§7) | mitigate | On a `purohit_id + normalized family_name` collision, confirmJob never silently merges — it pauses for an explicit "यह वही शर्मा परिवार है?" purohit confirmation before any write, preventing a wrong-family merge |
| T-02-24 | Spoofing | msg.from used for purohit lookup | accept | Same trust level Phase 1's onboarding already extends to inbound WhatsApp sender identity (Meta-verified webhook delivery) — no new risk introduced by routing audio/image/interactive through the same lookup |
</threat_model>

<verification>
- `pnpm --dir code --filter @workspace/api-server run typecheck` passes
- `npx playwright test ingest` passes (3 tests)
- `npx playwright test whatsapp` still passes unmodified (existing text-message contract preserved)
</verification>

<success_criteria>
- confirmJob is the sole yajmans/events write path in the entire phase (ING-03, structurally proven)
- Webhook accepts audio/image/interactive with immediate 200, routes correctly into the pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/02-bahi-khata-ingestion/02-08-SUMMARY.md`
</output>
