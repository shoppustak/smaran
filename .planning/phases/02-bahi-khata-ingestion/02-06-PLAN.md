---
phase: 02-bahi-khata-ingestion
plan: 06
type: execute
wave: 2
depends_on: ["02-05"]
files_modified:
  - code/artifacts/api-server/src/lib/confirm-card.ts
  - code/artifacts/api-server/src/lib/confirm-card.test.ts
autonomous: true
requirements: [ING-01, ING-02]

must_haves:
  truths:
    - "Every confirm card renders below-threshold fields with a ❓ prompt and 3 nearest candidates, never silently presenting a low-confidence guess as fact"
    - "The confirm card's locked Hindi copy (✓ सही है / ✏ सुधारें, ❓-gating) matches §6 verbatim"
  artifacts:
    - path: code/artifacts/api-server/src/lib/confirm-card.ts
      provides: "buildConfirmCard / sendConfirmCard / topCandidates / shouldAskField / buildMultiFamilyFollowup"
  key_links:
    - from: code/artifacts/api-server/src/lib/confirm-card.ts
      to: code/artifacts/api-server/src/lib/whatsapp-client.ts
      via: "sendWhatsappMessage() call"
      pattern: "sendWhatsappMessage\\("
---

<objective>
Build Stage G's confirm card per `docs/ref-state-2-plan.md` §6 — the interactive WhatsApp message
that renders extracted fields for a one-tap confirm, gates below-threshold fields with a ❓ prompt
and 3 nearest canonical candidates, and handles the multi-family-note follow-up.

Purpose: §6's confirm-card UX is locked verbatim by CONTEXT.md and is the single most
user-facing artifact this phase produces — "the AI drafts, the priest ratifies" happens here.
Output: `confirm-card.ts` with its locked Hindi copy, a passing unit test for its pure gating/
candidate-ranking logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bahi-khata-ingestion/02-CONTEXT.md
@docs/ref-state-2-plan.md

<interfaces>
Exact §6 card layout and copy (verbatim, locked — do not paraphrase):
```
📿 नया परिवार — पुष्टि करें
परिवार: शर्मा परिवार
गोत्र: कश्यप
१. श्राद्ध — पिताजी — भाद्रपद, कृष्ण पक्ष, द्वादशी
२. जन्मदिन — बेटी — कार्तिक, शुक्ल, पंचमी
[✓ सही है]  [✏ सुधारें]
```
- All fields >= threshold: card as above. `✓` writes everything (Stage H). `✏` opens a numbered
  list — reply with the item number, then a WhatsApp interactive list message offering the 3
  nearest canonical candidates + "कुछ और" (free-text re-entry for that field only).
- Any field < threshold: that field renders with `❓` and the card leads with the clarifying
  question (e.g. "माह ठीक से सुन नहीं पाया — भाद्रपद / आश्विन / कुछ और?" as list options). Never
  write a below-threshold field silently.
- Multi-family note: confirm family #1's card + one line:
  "दूसरे परिवार के लिए एक और voice note भेज दें 🙏".
- Re-record path: the purohit can always just send a new voice note; the newest note for an
  unconfirmed job supersedes (handled by the webhook-wiring plan, not here).

`whatsapp-client.ts`'s `sendWhatsappMessage(to, payload)` signature (plan 02-05 output):
```typescript
sendWhatsappMessage(to: string, payload: { type: "text"; text: { body: string } } | { type: "interactive"; interactive: Record<string, unknown> }): Promise<{ status: "sent"; messageId: string }>
```

`ExtractionResultSchema`'s shape (plan 02-04 output) is what `buildConfirmCard` renders fields from.
`fuzzy-match.ts`'s `matchField` (plan 02-02 output) returns only the single best match — this plan
needs a local `topCandidates` helper returning the top-N, kept inside `confirm-card.ts` rather than
widening `fuzzy-match.ts`'s public surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: topCandidates + field-threshold-gating pure logic</name>
  <files>code/artifacts/api-server/src/lib/confirm-card.ts, code/artifacts/api-server/src/lib/confirm-card.test.ts</files>
  <read_first>
    - code/artifacts/api-server/src/lib/fuzzy-match.ts (plan 02-02 output — reuse its Levenshtein logic, don't duplicate the DP table)
    - code/artifacts/api-server/src/lib/vocab/maas.ts (a VocabEntry[] to test against)
    - docs/ref-state-2-plan.md §6 ("3 nearest canonical candidates" and "All fields >= threshold" rules, see interfaces block above)
  </read_first>
  <behavior>
    - Test: topCandidates("भदो", maasVocab, 3) returns exactly 3 canonical entries sorted by ascending edit distance, closest first
    - Test: shouldAskField(0.4, 0.7) returns true (score below threshold — needs a clarifying question)
    - Test: shouldAskField(0.7, 0.7) returns false (score exactly at threshold counts as confident, per §6 "All fields >= threshold")
  </behavior>
  <action>
    Implement `topCandidates(heard: string, vocab: VocabEntry[], n: number): string[]` in
    `confirm-card.ts` — reuse `fuzzy-match.ts`'s internal Levenshtein computation (export it as a
    second named export from `fuzzy-match.ts` if that keeps this file clean, rather than
    re-implementing the DP table here) to rank all vocab entries by edit distance ascending and
    return the top `n` canonical strings. Implement `shouldAskField(score: number, threshold: number): boolean`
    returning `score < threshold`. Add both functions' tests to the existing
    `pnpm --dir code --filter @workspace/api-server run test:unit` glob (already wired in plan 02-02's
    package.json script — no new script needed).
  </action>
  <acceptance_criteria>
    - `pnpm --dir code --filter @workspace/api-server run test:unit` exits 0 with all 3 behavior cases passing
    - `grep -c "export.*topCandidates" code/artifacts/api-server/src/lib/confirm-card.ts` = 1
    - `grep -c "export.*shouldAskField" code/artifacts/api-server/src/lib/confirm-card.ts` = 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run test:unit</automated>
  </verify>
  <done>topCandidates/shouldAskField committed and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: buildConfirmCard / sendConfirmCard / buildMultiFamilyFollowup wiring</name>
  <files>code/artifacts/api-server/src/lib/confirm-card.ts</files>
  <read_first>
    - code/artifacts/api-server/src/lib/whatsapp-client.ts (plan 02-05 output — the sendWhatsappMessage helper this must call)
    - code/artifacts/api-server/src/lib/extraction.ts (ExtractionResultSchema shape being rendered)
    - docs/ref-state-2-plan.md §6 (exact Hindi copy, see interfaces block above)
  </read_first>
  <action>
    Implement `buildConfirmCard(extraction: ExtractionResult, fieldScores: Record<string, number>, thresholds: Record<string, number>): { type: "interactive"; interactive: Record<string, unknown> }`
    rendering §6's exact layout: family name line (`परिवार: {family_name}`), gotra line
    (`गोत्र: {gotra}`), numbered event lines using Devanagari numerals matching the literal example
    (`१. {event_type_hindi} — {label} — {maas}, {paksha} पक्ष, {tithi_name}`), and
    `[✓ सही है] [✏ सुधारें]` as WhatsApp interactive button-reply options with reply ids
    `confirm:{jobId}` / `edit:{jobId}`. For each field where `shouldAskField(fieldScores[field],
    thresholds[field])` is true, render that field with a `❓` prefix instead of its value and lead
    the card with the clarifying question + a WhatsApp interactive list message using
    `topCandidates(...)` for that field's 3 nearest canonical entries plus a `"कुछ और"` free-text
    fallback option. Implement `sendConfirmCard(to: string, card: ReturnType<typeof buildConfirmCard>): Promise<void>`
    calling `whatsapp-client.ts`'s `sendWhatsappMessage(to, card)`. Implement
    `buildMultiFamilyFollowup(): { type: "text"; text: { body: string } }` returning the exact
    "दूसरे परिवार के लिए एक और voice note भेज दें 🙏" line per §6's multi-family case.
  </action>
  <acceptance_criteria>
    - `grep -c "सही है" code/artifacts/api-server/src/lib/confirm-card.ts` >= 1
    - `grep -c "सुधारें" code/artifacts/api-server/src/lib/confirm-card.ts` >= 1
    - `grep -c "❓" code/artifacts/api-server/src/lib/confirm-card.ts` >= 1
    - `grep -c "कुछ और" code/artifacts/api-server/src/lib/confirm-card.ts` >= 1
    - `grep -c "sendWhatsappMessage" code/artifacts/api-server/src/lib/confirm-card.ts` >= 1
    - `pnpm --dir code --filter @workspace/api-server run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>confirm-card.ts committed with locked §6 Hindi copy verbatim, calls sendWhatsappMessage via the shared helper, field-level ❓-gating implemented per threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| extraction/fuzzy-match output (untrusted-confidence AI draft) -> confirm-card rendering | the last checkpoint before the purohit sees the draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | confirm-card field-gating (shouldAskField) | mitigate | A field below threshold is never rendered as a confident value — always shown with ❓ and routed through the clarifying list-message flow, preventing a low-confidence AI guess from being one-tap-confirmed as if it were certain |
| T-02-13 | Repudiation | ✓/✏ button-reply payload | accept | WhatsApp interactive button replies carry no server-verifiable signature beyond Meta's own webhook delivery — accepted, matching the same trust level Phase 1 already extends to all inbound WhatsApp messages |
</threat_model>

<verification>
- `pnpm --dir code --filter @workspace/api-server run test:unit` passes
- `pnpm --dir code --filter @workspace/api-server run typecheck` passes
</verification>

<success_criteria>
- buildConfirmCard renders §6's exact locked copy and threshold-gates every field
- confirm-card.ts sends through the shared whatsapp-client helper, no duplicated fetch logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-bahi-khata-ingestion/02-06-SUMMARY.md`
</output>
