---
phase: 06-family-calendar-subscription
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - code/artifacts/api-server/src/lib/confirm-card.ts
  - code/artifacts/api-server/src/routes/whatsapp.ts
  - tests/api/subscription.spec.ts
autonomous: true
requirements: [FAM-01, FAM-03]

must_haves:
  truths:
    - "Family subscription offer card renders correctly using Devanagari Hindi text and contains the Autopay deep link"
    - "Yajman contacts are verified against relationships to prevent cross-purohit data access"
    - "Attempts by unauthorized contacts to access other records are blocked"
  artifacts:
    - path: code/artifacts/api-server/src/lib/confirm-card.ts
      provides: "buildFamilyCalendarOfferCard"
    - path: code/artifacts/api-server/src/routes/whatsapp.ts
      provides: "Family-side webhook routing and tenant-isolation validations"
    - path: tests/api/subscription.spec.ts
      provides: "E2E integration test suite validating subscription creation and isolation"
  key_links:
    - from: code/artifacts/api-server/src/routes/whatsapp.ts
      to: code/artifacts/api-server/src/lib/subscription.ts
      via: "buildAutopayDeepLink invocation"
      pattern: "buildAutopayDeepLink\\("
---

<objective>
Design and implement the family subscription offer WhatsApp cards, add strict purohit-isolation checking in webhook routes to prevent cross-purohit leakages, and write E2E validation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-family-calendar-subscription/06-CONTEXT.md
@.planning/phases/06-family-calendar-subscription/06-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Family Subscription Interactive Card</name>
  <files>code/artifacts/api-server/src/lib/confirm-card.ts</files>
  <action>
    Modify `code/artifacts/api-server/src/lib/confirm-card.ts` to export:
    `buildFamilyCalendarOfferCard(yajmanId: string, purohitName: string, VPA: string): { type: "interactive"; interactive: Record<string, unknown> }`.
    It constructs a button-interactive card:
    - Body:
      `📅 अपने परिवार का पंचांग और अनुष्ठान कैलेंडर सीधे अपने WhatsApp पर प्राप्त करें।\n(वार्षिक अनुष्ठानों के स्मरण पत्र सीधे प्राप्त होंगे)\n\nशुल्क: ₹29/माह (UPI Autopay)\nपुरोहित जी: ${purohitName}`
    - Button: `सदस्यता लें` pointing to the recurring mandate URL returned by `buildAutopayDeepLink`.
  </action>
  <acceptance_criteria>
    - Card builder exports correctly.
    - Autopay mandate URL includes correct params.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Family subscription card builder written.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Webhook Subscriptions and Isolation Gating</name>
  <files>code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <read_first>
    - code/artifacts/api-server/src/routes/whatsapp.ts
  </read_first>
  <action>
    Modify the WhatsApp webhook POST route `/whatsapp/webhook`:
    1. Handle simulated mandate-activation triggers.
    2. Enforce the isolation assertion (FAM-03): whenever any family number messages or queries an event, load their `yajman` record from the database. Assert that `yajman.purohitId` matches the requested resource context. If a mismatch is matched, abort the execution (log alert, return HTTP 403 / warning reply).
    3. Update family subscription status to `active` and extend `familySubRenewsAt` by 30 days upon receiving activation webhooks.
  </action>
  <acceptance_criteria>
    - Relationship isolation check is executed on all incoming family calls.
    - Activations successfully save to PostgreSQL.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Webhook subscription routing and isolation checks completed.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Playwright E2E Spec</name>
  <files>tests/api/subscription.spec.ts</files>
  <action>
    Create `tests/api/subscription.spec.ts` containing Playwright integration tests:
    1. Seed a mock purohit, yajman, and event.
    2. Verify sending a subscription offer card to the yajman contact.
    3. Simulate webhook sub activation and verify that `familySubStatus` transitions to `active` in Postgres.
    4. Isolation Test: Simulate a family contact attempting to query or confirm an event associated with a different purohit. Verify that access is blocked and the request is rejected.
  </action>
  <acceptance_criteria>
    - Specs run and successfully verify sub logic and tenant-isolation constraints.
    - `DATABASE_URL="..." INTERNAL_API_KEY="..." npx playwright test subscription` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>DB_PASS=$(awk '/### smaran-dev/{f=1} f && /db pass/{print $NF; exit}' docs/db-creds) && DATABASE_URL="postgresql://postgres.ajzoxgjvtxhzzkgxxmxv:${DB_PASS}@aws-1-ap-south-1.pooler.supabase.com:6543/postgres" INTERNAL_API_KEY="e2e-local-key" npx playwright test subscription</automated>
  </verify>
  <done>Playwright E2E spec verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Cross-purohit data discovery | mitigate | Enforce strict relationship checks: a yajman number can only query or confirm event/ledger links that are owned by their direct associated purohit |
</threat_model>

<verification>
- Playwright E2E spec passes green.
</verification>

<success_criteria>
- Autopay cards generate dynamically.
- Strictly no cross-purohit leaks.
</success_criteria>

<output>
After completion, create `.planning/phases/06-family-calendar-subscription/06-02-SUMMARY.md`
</output>
