---
phase: 06-family-calendar-subscription
plan: 01
type: execute
wave: 1
depends_on: ["05-02"]
files_modified:
  - code/artifacts/api-server/src/lib/subscription.ts
autonomous: true
requirements: [FAM-01, FAM-02]

must_haves:
  truths:
    - "Autopay mandate deep links are generated using the upi://mandate scheme with recur=MONTHLY and am=29.00 parameters"
    - "The VPA belongs to the associated purohit configure during onboarding"
    - "A background status-update function transitions active expired subscriptions to lapsed status"
    - "All database access in subscription.ts uses dynamic gating"
  artifacts:
    - path: code/artifacts/api-server/src/lib/subscription.ts
      provides: "buildAutopayDeepLink, runSubscriptionStateCheck"
---

<objective>
Implement the dynamic UPI Autopay mandate URI builder in `subscription.ts`, and write the background database state transition logic that identifies expired subscriptions and marks them as lapsed.
All database access is dynamically imported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-family-calendar-subscription/06-CONTEXT.md
@.planning/phases/06-family-calendar-subscription/06-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build UPI Autopay mandate builder</name>
  <files>code/artifacts/api-server/src/lib/subscription.ts</files>
  <action>
    Create `code/artifacts/api-server/src/lib/subscription.ts`.
    Implement `buildAutopayDeepLink(yajmanId: string, merchantVpa: string, payeeName: string): string`:
    Construct mandate URI string:
    `upi://mandate?pa=${encodeURIComponent(merchantVpa)}&pn=${encodeURIComponent(payeeName)}&am=29.00&cu=INR&mc=8999&tr=${yajmanId}&recur=MONTHLY`
  </action>
  <acceptance_criteria>
    - `buildAutopayDeepLink` is exported.
    - Mandate link parameters match standard specifications.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Autopay mandate link utility built.</done>
</task>

<task type="auto">
  <name>Task 2: Build Expired Subscription Checker</name>
  <files>code/artifacts/api-server/src/lib/subscription.ts</files>
  <action>
    Implement `runSubscriptionStateCheck(): Promise<void>`:
    Using dynamic imports of `@workspace/db`, scan yajman records.
    Update `familySubStatus` to `"lapsed"` for all rows where `familySubStatus === "active"` and `familySubRenewsAt < new Date()`.
  </action>
  <acceptance_criteria>
    - `runSubscriptionStateCheck` is exported.
    - Expired yajmans update successfully.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>State updates helper built.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Redirection of Autopay VPA | mitigate | Retrieve VPA directly from the associated purohit's DB record during mandate generation |
</threat_model>

<verification>
- Typechecking passes cleanly.
</verification>

<success_criteria>
- Autopay deep links format correctly.
- Lapsed states update in database.
</success_criteria>

<output>
After completion, create `.planning/phases/06-family-calendar-subscription/06-01-SUMMARY.md`
</output>
