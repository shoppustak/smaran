---
phase: 05-schedule-protection
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - code/artifacts/api-server/src/routes/whatsapp.ts
  - tests/api/schedule.spec.ts
autonomous: true
requirements: [PROT-01, PROT-02]

must_haves:
  truths:
    - "Incoming text messages 'my week' or 'इस हफ्ते' are matched on the webhook route"
    - "Day sheets return list of events for the coming week grouped by date and window"
    - "Colliding confirmJob calls return an interactive warning card to the purohit"
    - "Override action buttons are verified for caller ownership"
    - "Cancel action buttons update state to rejected/cancelled"
  artifacts:
    - path: code/artifacts/api-server/src/routes/whatsapp.ts
      provides: "Day-sheet command route and booking override action routing"
    - path: tests/api/schedule.spec.ts
      provides: "E2E specs verifying day-sheet text formats and collision override flows"
---

<objective>
Implement the weekly day-sheet reporting commands, configure the webhook router to return collision warning override cards on overlaps, and write E2E integration specs.
This enables purohits to view schedules and resolve scheduling overlaps interactively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-schedule-protection/05-CONTEXT.md
@.planning/phases/05-schedule-protection/05-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Day-Sheet Reporting Command</name>
  <files>code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <action>
    Modify text parser in `routes/whatsapp.ts`.
    1. Intercept incoming text messages matching `"my week"` or `"इस हफ्ते"`.
    2. Retrieve the active purohit's events in the coming 7 days.
    3. Group events by date and format the output text message:
       - Header: `📅 आपका साप्ताहिक कार्यक्रम:`
       - Group Header: Gregorian Date (e.g. `15-07-2026 (बुधवार)`)
       - Format: `[window_emoji] [time] - [event_label] (यजमान: [family_name])`
    4. Send the formatted text message back via WhatsApp.
  </action>
  <acceptance_criteria>
    - Matches both English and Hindi trigger text.
    - Groups events by date and window.
    - Resolves and formats Hindi day names.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Day-sheet command wired.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Webhook Override & Cancel Actions</name>
  <files>code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <action>
    Modify webhook routes:
    1. If `confirmJob` throws `MuhuratCollisionError`, catch it.
    2. Send an interactive button message back:
       - Body: `⚠️ चेतावनी: इस समय पर पहले से एक अनुष्ठान बुक है। क्या आप फिर भी इसे सहेजना चाहते हैं?`
       - Buttons: `हाँ, सहेजें` (action ID `booking-force:${jobId}`) and `रद्द करें` (action ID `booking-cancel:${jobId}`).
    3. Route callback action `booking-force:${jobId}` to call `confirmJob(jobId, purohitId, { force: true })`. On success, send: `"अनुष्ठान बुक कर लिया गया है (ओवरराइड)।"`.
    4. Route callback action `booking-cancel:${jobId}` to call `rejectJob(jobId)`. Send: `"अनुष्ठान रद्द कर दिया गया है।"`.
  </action>
  <acceptance_criteria>
    - Webhook sends override prompt on conflict.
    - booking-force action overrides the collision check.
    - booking-cancel rejects the job.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Webhook warning overrides wired.</done>
</task>

<task type="auto">
  <name>Task 3: Write Playwright E2E Spec</name>
  <files>tests/api/schedule.spec.ts</files>
  <action>
    Create `tests/api/schedule.spec.ts`.
    Write tests validating:
    1. Sending `"my week"` text command and verifying the grouped schedule formats in response.
    2. Creating overlapping events. Asserting that confirming the second event returns the warning button card.
    3. Sending the `booking-force` button tap webhook. Asserting that the overlap is bypassed and both events are saved to the database.
    4. Sending the `booking-cancel` button tap webhook. Asserting that the job is rejected.
  </action>
  <acceptance_criteria>
    - E2E tests run and verify all protecting scenarios.
    - `DATABASE_URL="..." INTERNAL_API_KEY="..." npx playwright test schedule` passes green.
  </acceptance_criteria>
  <verify>
    <automated>DB_PASS=$(awk '/### smaran-dev/{f=1} f && /db pass/{print $NF; exit}' docs/db-creds) && DATABASE_URL="postgresql://postgres.ajzoxgjvtxhzzkgxxmxv:${DB_PASS}@aws-1-ap-south-1.pooler.supabase.com:6543/postgres" INTERNAL_API_KEY="e2e-local-key" npx playwright test schedule</automated>
  </verify>
  <done>Schedule protection Playwright E2E spec verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation of Privilege | Day-sheet command | mitigate | Filter upcoming events strictly by the sender's resolved purohit ID, preventing retrieval of other purohits' schedules |
| T-05-05 | Elevation of Privilege | booking-force action | mitigate | Verify caller ownership relative to the job record before executing force override confirmations |
</threat_model>

<verification>
- Playwright E2E spec passes green.
</verification>

<success_criteria>
- Collision warnings are interactive and overrideable.
- Weekly day-sheets return clear formatting.
</success_criteria>

<output>
After completion, create `.planning/phases/05-schedule-protection/05-02-SUMMARY.md`
</output>
