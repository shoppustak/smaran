---
phase: 05-schedule-protection
plan: 01
type: execute
wave: 1
depends_on: ["04-03"]
files_modified:
  - code/artifacts/api-server/src/lib/muhurat.ts
  - code/artifacts/api-server/src/lib/ingest.ts
autonomous: true
requirements: [PROT-02]

must_haves:
  truths:
    - "A day is divided into Morning (06:00-12:00), Afternoon (12:00-16:00), Evening (16:00-20:00), and Night (20:00-06:00) segments"
    - "Collision checks detect overlapping events for the same purohit on the same Gregorian date"
    - "Unforced database confirmations throw MuhuratCollisionError on overlap"
    - "All database queries in muhurat.ts are dynamically gated"
  artifacts:
    - path: code/artifacts/api-server/src/lib/muhurat.ts
      provides: "resolveMuhuratWindow, hasMuhuratCollision"
  key_links:
    - from: code/artifacts/api-server/src/lib/ingest.ts
      to: code/artifacts/api-server/src/lib/muhurat.ts
      via: "hasMuhuratCollision check"
      pattern: "hasMuhuratCollision\\("
---

<objective>
Implement the core Muhurat collision checks in `muhurat.ts` and integrate them into the ingestion write gate (`confirmJob` in `ingest.ts`).
This ensures bookings cannot be double-booked by default unless forced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-schedule-protection/05-CONTEXT.md
@.planning/phases/05-schedule-protection/05-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Muhurat Parsing and Collision Utilities</name>
  <files>code/artifacts/api-server/src/lib/muhurat.ts</files>
  <action>
    Create `code/artifacts/api-server/src/lib/muhurat.ts`.
    1. Define custom error type `MuhuratCollisionError` carrying the conflicting event's title and details.
    2. Implement `resolveMuhuratWindow(timeStr: string): "morning" | "afternoon" | "evening" | "night"` parsing standard time string (HH:MM) to map boundaries:
       - Morning: 06:00 - 12:00
       - Afternoon: 12:00 - 16:00
       - Evening: 16:00 - 20:00
       - Night: 20:00 - 06:00
    3. Implement `hasMuhuratCollision(purohitId: string, gregorianDate: Date, timeStr: string): Promise<boolean>` using dynamic imports:
       - Query all database events scheduled on the target date for this purohit.
       - Assert if any event maps to the same resolved Muhurat window as the target `timeStr`.
  </action>
  <acceptance_criteria>
    - Functions are fully exported.
    - Matches overlapping windows correctly.
    - Typecheck compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Muhurat parsing and collision utilities built.</done>
</task>

<task type="auto">
  <name>Task 2: Hook Collision Checks in Ingestion Write Gate</name>
  <files>code/artifacts/api-server/src/lib/ingest.ts</files>
  <read_first>
    - code/artifacts/api-server/src/lib/ingest.ts
  </read_first>
  <action>
    Modify `confirmJob` in `ingest.ts` to accept an optional parameter `force: boolean = false`.
    1. Before writing new events to PostgreSQL, run `hasMuhuratCollision(...)` using the event's date and scheduled time.
    2. If a collision is matched and `force` is set to false, throw `MuhuratCollisionError`.
    3. If `force` is true, bypass the check and log an override entry.
  </action>
  <acceptance_criteria>
    - `confirmJob` checks for collisions.
    - Throws `MuhuratCollisionError` when overlaps occur.
    - Bypasses check when `force` is true.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Collision checking hooked into confirmJob write gate.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Bypassing collision queries | mitigate | Enforce that overrides require explicit force parameters and can only be triggered via user-confirmed action buttons |
</threat_model>

<verification>
- Typechecking passes cleanly.
</verification>

<success_criteria>
- Overlapping bookings throw errors by default.
- Force flags enable bypass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-schedule-protection/05-01-SUMMARY.md`
</output>
