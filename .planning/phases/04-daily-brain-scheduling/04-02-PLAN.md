---
phase: 04-daily-brain-scheduling
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - code/artifacts/api-server/src/lib/brain.ts
  - code/artifacts/api-server/src/lib/confirm-card.ts
  - code/artifacts/api-server/src/routes/whatsapp.ts
autonomous: true
requirements: [BRAIN-01, BRAIN-03]

must_haves:
  truths:
    - "Cards sent for upcoming rituals do not contain payment gateway or PSP links"
    - "Pre-ritual cards contain a Confirm booking interactive action button"
    - "Solemn (shraddh) and celebratory (birthday, katha) events use separate templates with different language registers (solemn vs. festive)"
    - "Confirming a booking updates the event's booking state in the database"
  artifacts:
    - path: code/artifacts/api-server/src/lib/confirm-card.ts
      provides: "buildUpcomingPreRitualCard"
    - path: code/artifacts/api-server/src/lib/brain.ts
      provides: "sendPreRitualAlerts"
  key_links:
    - from: code/artifacts/api-server/src/routes/whatsapp.ts
      to: code/artifacts/api-server/src/lib/confirm-card.ts
      via: "buildUpcomingPreRitualCard invocation"
      pattern: "buildUpcomingPreRitualCard\\("
---

<objective>
Implement pre-ritual interactive notification builders with distinct language registers for solemn and celebratory events. Wire up booking confirmation button callbacks in the webhook router.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-daily-brain-scheduling/04-CONTEXT.md
@.planning/phases/04-daily-brain-scheduling/04-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Pre-Ritual Cards with Separate Copy Registers</name>
  <files>code/artifacts/api-server/src/lib/confirm-card.ts</files>
  <action>
    Modify `code/artifacts/api-server/src/lib/confirm-card.ts` to export:
    `buildUpcomingPreRitualCard(event: ResolvedBrainEvent, daysRemaining: number): { type: "interactive"; interactive: Record<string, unknown> }`.
    Implement copy registers:
    1. Solemn (e.g. `event.eventType === "shraddh"`):
       Body: `आदरणीय पुरोहित जी, यजमान ${event.familyName} के परिवार का ${event.maas} ${event.paksha} ${event.tithi} को श्राद्ध अनुष्ठान है (शेष दिन: ${daysRemaining})। क्या आप इस तिथि को उपलब्ध हैं?`
       Button: `हाँ, बुक करें` carrying action ID `booking-confirm:${event.id}`.
    2. Celebratory (all other event types):
       Body: `प्रणाम पुरोहित जी! यजमान ${event.familyName} के परिवार का ${event.eventLabel} शुभ अवसर आ रहा है (शेष दिन: ${daysRemaining})। पूजा की पुष्टि करें:`
       Button: `हाँ, बुक करें` carrying action ID `booking-confirm:${event.id}`.
  </action>
  <acceptance_criteria>
    - Solemn event template uses highly formal language.
    - No payment links exist in either template.
    - Buttons carry the exact `booking-confirm:${eventId}` action ID.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Pre-ritual templates implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Notification Dispatcher and Webhook Confirms</name>
  <files>code/artifacts/api-server/src/lib/brain.ts, code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <action>
    1. Implement `sendPreRitualAlerts(event: ResolvedBrainEvent, daysRemaining: number): Promise<void>` in `brain.ts`. Send pre-ritual alert cards using `sendWhatsappMessage` to the associated purohit.
    2. Update the webhook router in `routes/whatsapp.ts` to handle interactive button callback `booking-confirm:{eventId}`:
       - Verify that the sender is the associated purohit.
       - Create a ledger entry for this event (with status `pending`) to transition to the payment phase (Phase 3 integration).
       - Respond with confirmation text: `"अनुष्ठान बुक कर लिया गया है। कार्यक्रम में जोड़ दिया गया है।"`.
  </action>
  <acceptance_criteria>
    - Webhook routes `booking-confirm:*` correctly.
    - Creates a ledger entry when booking is confirmed.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Alert dispatches and webhook actions wired.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Elevation of Privilege | booking-confirm webhook | mitigate | Verify that the sender's phone number matches the associated purohit before processing the booking |
</threat_model>

<verification>
- Typechecking passes cleanly.
</verification>

<success_criteria>
- Separate registers are applied.
- Webhooks handle confirmations successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/04-daily-brain-scheduling/04-02-SUMMARY.md`
</output>
