---
phase: 04-daily-brain-scheduling
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - code/artifacts/api-server/src/lib/brain.ts
  - code/artifacts/api-server/src/lib/confirm-card.ts
  - code/artifacts/api-server/src/routes/whatsapp.ts
  - code/artifacts/api-server/src/lib/whatsapp-client.ts
  - code/lib/db/src/schema/lapse-recoveries.ts
  - code/lib/db/src/schema/index.ts
  - render.yaml
  - tests/api/brain.spec.ts
autonomous: false
requirements: [BRAIN-01, BRAIN-02, BRAIN-03]

# Gap-closure plan. Phases 04-01..04-03 shipped green tests but the tests
# validated a narrowed reading of the plans, not the ROADMAP. A goal-backward
# audit (2026-07-16) found 5 roadmap gaps + code smells. This plan closes them.
# Each task cites the exact ROADMAP Phase 4 success criterion it restores.

must_haves:
  truths:
    - "The pre-ritual card actually dispatched by the cron contains the samagri (material) checklist"
    - "Pre-ritual alerts fire only at the ~7-day and ~2-day marks, not on every day inside the 7-day window"
    - "A daily scheduled trigger invokes POST /cron/daily-brain at 06:00 IST without manual action"
    - "The lapse-engage button has a webhook handler and a recovery is recorded in a durable store, not only a log line"
    - "Proactive (business-initiated) sends use Meta-approved message templates, with free-form interactive only as an inside-24h fallback"
    - "brain.spec.ts asserts samagri presence, 7/2 cadence gating, and lapse-recovery tracking"
    - "The orphaned sendPreRitualAlert function and duplicated Hindi vocab maps are removed"
  artifacts:
    - path: code/lib/db/src/schema/lapse-recoveries.ts
      provides: "lapse_recoveries table (durable recovery metric store)"
    - path: code/artifacts/api-server/src/lib/whatsapp-client.ts
      provides: "sendWhatsappTemplate helper"
    - path: render.yaml
      provides: "daily 06:00 IST cron trigger for /cron/daily-brain"
  key_links:
    - from: code/artifacts/api-server/src/lib/brain.ts
      to: code/artifacts/api-server/src/lib/confirm-card.ts
      via: "buildUpcomingPreRitualCard invocation (single card path, samagri-bearing)"
      pattern: "buildUpcomingPreRitualCard\\("
    - from: code/artifacts/api-server/src/routes/whatsapp.ts
      to: code/lib/db/src/schema/lapse-recoveries.ts
      via: "lapse-engage handler records recovery"
      pattern: "lapse-engage"
---

<objective>
Close the 5 roadmap gaps found by the 2026-07-16 goal-backward audit of Phase 4, so the
Daily Brain actually satisfies ROADMAP Phase 4 success criteria 1–3 (not just the narrower
plan text that 04-01..04-03 implemented). Also remove the dead/duplicated code the audit
surfaced. Tests are extended to assert the roadmap behaviours that were previously unchecked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-daily-brain-scheduling/04-CONTEXT.md
@.planning/phases/04-daily-brain-scheduling/04-PATTERNS.md
@.planning/phases/04-daily-brain-scheduling/04-01-SUMMARY.md
@.planning/phases/04-daily-brain-scheduling/04-02-SUMMARY.md
@.planning/phases/04-daily-brain-scheduling/04-03-SUMMARY.md
@docs/smaran-blueprint-v3.md
</context>

<audit_findings>
## Gaps this plan closes (ROADMAP Phase 4 = ground truth; code verified 2026-07-16)

| # | Roadmap criterion | Gap in shipped code | Severity |
|---|-------------------|---------------------|----------|
| G1 | C1: "daily 6:00 AM job" | No scheduler exists. Only POST /cron/daily-brain; render.yaml has no cron. Nothing fires it daily. | HIGH |
| G2 | C1: "cards ~7 and ~2 days ahead" | resolveUpcomingEventsForWeek returns whole 7-day window every run; no gating to 7 & 2. Daily run → 7× spam; weekly run → misses 2-day. | HIGH |
| G3 | C1: "samagri list" | Live path (dispatchPreRitualAlerts → sendPreRitualAlerts → buildUpcomingPreRitualCard) sends NO samagri. Samagri-rich sendPreRitualAlert (brain.ts) is orphaned dead code. | HIGH |
| G4 | C2: "each recovery counted as a tracked metric" | Only logger.info on nudge dispatch; no durable counter. lapse-engage button has NO webhook handler → a recovery is never detectable/countable. | HIGH |
| G5 | C3: "template packs" (+ Phase 1 C4 registered Utility templates) | Proactive sends use free-form interactive via sendWhatsappMessage → will NOT deliver in prod outside the 24h window. No sendTemplate helper exists. | HIGH (prod) |

Code smells (not roadmap gaps, fixed opportunistically): orphaned sendPreRitualAlert; Hindi
vocab maps duplicated across brain.ts and confirm-card.ts; button-id inconsistency
(confirm-ritual: in dead fn vs live booking-confirm:). brain.spec.ts asserts none of the above.
</audit_findings>

<tasks>

<task type="auto">
  <name>Task 1: Single samagri-bearing card path; delete dead code (G3 + cleanup)</name>
  <files>code/artifacts/api-server/src/lib/confirm-card.ts, code/artifacts/api-server/src/lib/brain.ts</files>
  <action>
    1. Move the per-event-type samagri checklist (currently in the orphaned
       brain.ts:sendPreRitualAlert) INTO buildUpcomingPreRitualCard in confirm-card.ts, so the
       card the cron actually dispatches includes: Gregorian date, tithi/maas/paksha (Devanagari),
       ritual name, samagri list, and the booking-confirm:{eventId} button. No payment link.
    2. Delete the orphaned brain.ts:sendPreRitualAlert (singular) and its now-unused local Hindi
       maps. Keep exactly one Devanagari vocab source — reuse confirm-card.ts's toHindi helpers or
       a shared module; do not leave two copies of HINDI_MONTHS/PAKSHAS/TITHIS.
    3. Ensure buildUpcomingPreRitualCard is the ONLY pre-ritual card builder referenced by the
       dispatch path.
  </action>
  <acceptance_criteria>
    - The card returned by buildUpcomingPreRitualCard contains a non-empty samagri list for each event type.
    - grep shows no remaining definition of sendPreRitualAlert (singular).
    - Hindi vocab maps are defined once, not duplicated across files.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Live card carries samagri; dead duplicate code removed.</done>
</task>

<task type="auto">
  <name>Task 2: Gate alerts to the ~7-day and ~2-day marks (G2)</name>
  <files>code/artifacts/api-server/src/lib/brain.ts</files>
  <action>
    In the dispatch path (dispatchPreRitualAlerts / resolveUpcomingEventsForWeek), send a
    pre-ritual alert for an event only when its daysRemaining is at a configured touchpoint —
    default {7, 2}. Extract the touchpoints to a constant (e.g. PRE_RITUAL_ALERT_DAYS = [7, 2])
    so the "~" tolerance and cadence are explicit and testable. An event 5 or 6 days out must NOT
    be alerted; an event exactly 7 or 2 days out must be. This assumes a daily cron (Task 4).
  </action>
  <acceptance_criteria>
    - Events at daysRemaining 7 and 2 produce alerts; events at other offsets (e.g. 5, 3, 1) do not.
    - Touchpoints are a named constant, not a magic number inline.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Two-touchpoint cadence enforced.</done>
</task>

<task type="auto">
  <name>Task 3: lapse-engage handler + durable recovery metric (G4)</name>
  <files>code/lib/db/src/schema/lapse-recoveries.ts, code/lib/db/src/schema/index.ts, code/artifacts/api-server/src/lib/brain.ts, code/artifacts/api-server/src/routes/whatsapp.ts</files>
  <action>
    1. Add a lapse_recoveries table: id, event_id (fk), purohit_id (fk), yajman_id (fk),
       cycle_year (int), nudged_at (timestamptz), recovered_at (timestamptz nullable). Export from
       schema/index.ts. Push to smaran-dev (drizzle-kit push, same pattern as 01-01 / 02-01).
    2. In runLapseDetectionScan: on each nudge dispatch, upsert a lapse_recoveries row
       (event_id + cycle_year unique) with nudged_at — replacing the bare logger.info with a
       durable record. Keep the logger.info too.
    3. Add a webhook handler in whatsapp.ts for interactive reply id `lapse-engage:{eventId}`:
       verify the sender is the event's purohit (STRIDE T-04-04, mirrors booking-confirm's
       ownership check), then set the matching lapse_recoveries row's recovered_at = now() for the
       current cycle_year, and reply with a short Devanagari acknowledgement. This makes "each
       recovery counted" real: recovery = count(recovered_at IS NOT NULL).
    4. (Optional, if trivial) expose recovery count via the existing metrics route surface; not
       required for this plan's success.
  </action>
  <acceptance_criteria>
    - lapse_recoveries table exists in smaran-dev and is exported from schema/index.ts.
    - Nudge dispatch writes a durable lapse_recoveries row (nudged_at set).
    - Tapping lapse-engage from the correct purohit sets recovered_at; a non-owner sender is rejected.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Recovery is durably tracked and countable; lapse-engage button is live.</done>
</task>

<task type="checkpoint">
  <name>Task 4: Daily 06:00 IST scheduler (G1)</name>
  <files>render.yaml</files>
  <action>
    Add a scheduled trigger for POST /cron/daily-brain at 06:00 IST (00:30 UTC → cron "30 0 * * *").
    Implement as a Render cron job (or the project's chosen scheduler) that curls the api-server's
    /cron/daily-brain with header X-Cron-Secret: $CRON_SECRET. Document the required CRON_SECRET env
    on the api-server + cron services. This is a checkpoint because it needs the CRON_SECRET secret
    provisioned in the Render dashboard and a live service URL — confirm with the operator before/at
    apply time.
  </action>
  <acceptance_criteria>
    - render.yaml declares a daily 00:30 UTC job hitting /cron/daily-brain with the secret header.
    - CRON_SECRET is documented as a required env on both api-server and the cron job.
  </acceptance_criteria>
  <verify>
    <manual>Operator confirms the cron job appears in Render and fires; a manual trigger returns 200 and dispatches alerts.</manual>
  </verify>
  <done>Daily Brain runs itself at 6 AM without manual invocation.</done>
</task>

<task type="checkpoint">
  <name>Task 5: Proactive Meta template packs (G5)</name>
  <files>code/artifacts/api-server/src/lib/whatsapp-client.ts, code/artifacts/api-server/src/lib/brain.ts</files>
  <action>
    1. Register Meta-approved message templates (Utility category) for the proactive sends this
       phase makes: solemn pre-ritual, celebratory pre-ritual, and lapse-recovery nudge — each a
       separate, native-speaker-reviewed pack per ROADMAP C3. Capture template names + variable
       order in the KB.
    2. Add a sendWhatsappTemplate(to, templateName, components) helper in whatsapp-client.ts
       (none exists today).
    3. Route the brain's proactive dispatches through sendWhatsappTemplate. Free-form interactive
       (current buildUpcomingPreRitualCard) may remain only as the inside-24h-window fallback.
    Checkpoint: template approval is an external Meta process (days of lead time). Confirm with the
    operator whether to (a) block on approval, or (b) land the helper + wiring now behind a flag and
    approve templates in parallel. Product constraint: WhatsApp-only, no payment links — see
    code/.agents/memory/smaran-product.md before touching send paths.
  </action>
  <acceptance_criteria>
    - sendWhatsappTemplate helper exists and is used by proactive dispatch paths.
    - Template names/variables documented in the KB; three distinct register packs referenced.
    - Interactive free-form is fallback-only for proactive sends.
  </acceptance_criteria>
  <verify>
    <manual>Operator confirms templates submitted/approved in Meta; a proactive send outside the 24h window is delivered as a template.</manual>
  </verify>
  <done>Proactive sends are template-based and will deliver in production.</done>
</task>

<task type="auto">
  <name>Task 6: Extend brain.spec.ts to assert the restored roadmap behaviours</name>
  <files>tests/api/brain.spec.ts</files>
  <action>
    Add/extend E2E assertions (keep the existing no-raw-SQL-seeding style where possible):
    1. Samagri: the dispatched pre-ritual card body contains the samagri list (assert on a known
       marker line, e.g. the shraddh "काले तिल" entry).
    2. Cadence: seed events at 7-day and 2-day offsets → both alerted; seed one at a 5-day offset →
       NOT alerted.
    3. Lapse recovery: after a nudge, POST a lapse-engage:{eventId} webhook from the owning purohit
       → lapse_recoveries.recovered_at is set; a non-owner lapse-engage is rejected.
    Do NOT weaken assertions to make them pass — if a task above is incomplete, the test must fail.
  </action>
  <acceptance_criteria>
    - New assertions fail against pre-Task-1..3 code and pass after.
    - DATABASE_URL + CRON_SECRET E2E run exits 0.
  </acceptance_criteria>
  <verify>
    <automated>DB_PASS=$(awk '/### smaran-dev/{f=1} f && /db pass/{print $NF; exit}' docs/db-creds) && DATABASE_URL="postgresql://postgres.ajzoxgjvtxhzzkgxxmxv:${DB_PASS}@aws-1-ap-south-1.pooler.supabase.com:6543/postgres" CRON_SECRET="e2e-cron-secret" INTERNAL_API_KEY="e2e-local-key" npx playwright test brain</automated>
  </verify>
  <done>Tests now guard the roadmap behaviours, not the narrowed plan.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | lapse-engage webhook | mitigate | Verify sender phone number matches the event's purohit before setting recovered_at (mirror booking-confirm ownership check) |
| T-04-05 | Spoofing | render cron trigger | mitigate | X-Cron-Secret required on /cron/daily-brain; secret provisioned only in Render env |
| T-04-06 | Repudiation | recovery metric | mitigate | Durable lapse_recoveries rows (nudged_at, recovered_at) instead of ephemeral log lines |
</threat_model>

<verification>
- Typecheck passes cleanly across the code workspace.
- brain.spec.ts (extended) passes against smaran-dev, and its new assertions fail on the pre-fix code.
- Manual checkpoints (Tasks 4, 5) confirmed with the operator.
</verification>

<success_criteria>
- ROADMAP Phase 4 C1: the dispatched card carries tithi/date + ritual name + samagri + confirm tap, fired at ~7 and ~2 days, and a daily 6 AM trigger exists — no payment link.
- ROADMAP Phase 4 C2: lapse nudges dispatch AND each recovery is durably counted via lapse-engage.
- ROADMAP Phase 4 C3: proactive sends use separate template packs per register, delivering in production.
</success_criteria>

<output>
After completion, create `.planning/phases/04-daily-brain-scheduling/04-04-SUMMARY.md`
</output>
</content>
</invoke>
