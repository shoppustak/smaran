---
phase: 04-daily-brain-scheduling
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - code/artifacts/api-server/src/lib/brain.ts
  - tests/api/brain.spec.ts
autonomous: true
requirements: [BRAIN-02]

must_haves:
  truths:
    - "Lapse detection queries database for events where last_performed_year is older than the current year"
    - "Re-engagement notifications are sent to the associated purohit with action buttons"
    - "Recovery metrics are logged for ops tracking"
  artifacts:
    - path: tests/api/brain.spec.ts
      provides: "Playwright integration specs verifying cron triggers and recovery flows"
---

<objective>
Implement the lapse-detection scanner that surfaces quiet families who have missed their annual rituals and alerts the purohit. Validate the entire Daily Brain engine via Playwright E2E integration specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-daily-brain-scheduling/04-CONTEXT.md
@.planning/phases/04-daily-brain-scheduling/04-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Lapse-Detection Scanner</name>
  <files>code/artifacts/api-server/src/lib/brain.ts</files>
  <action>
    Implement `runLapseDetectionScan(): Promise<void>` in `brain.ts`.
    1. Query all active `events` records where `last_performed_year` is less than the current year.
    2. Check if a booking or confirmed ledger entry already exists for this event in the current Gregorian calendar year.
    3. If no current cycle booking is found, send a "re-engagement nudge" WhatsApp message to the associated purohit:
       `शर्मा जी, यजमान ${familyName} के परिवार का ${eventLabel} (${maas} ${paksha} ${tithi}) इस वर्ष अभी बुक नहीं हुआ है। क्या आप उन्हें संपर्क करना चाहते हैं?`
       Provide button: `नियत करें` carrying action ID `lapse-engage:${eventId}`.
    4. Log metrics to the database or logging transport.
  </action>
  <acceptance_criteria>
    - Lapsed events correctly identified.
    - Re-engagement card contains correct `lapse-engage` ID.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Lapse detection scanner implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Write Playwright E2E spec for Daily Brain</name>
  <files>tests/api/brain.spec.ts</files>
  <action>
    Create `tests/api/brain.spec.ts`.
    Write tests validating:
    1. Seeding database with upcoming events (matching a mock Vedika Panchang date payload) and a lapsed event.
    2. Sending a POST query to `/cron/daily-brain` with valid headers. Asserting status is 200.
    3. Asserting that pre-ritual alert cards and lapse nudge cards are successfully sent to the mocked WhatsApp handler.
    4. Gating checks: Assert that requests with incorrect cron secrets return status 401.
  </action>
  <acceptance_criteria>
    - Spec passes green on local test runner.
    - `DATABASE_URL="..." INTERNAL_API_KEY="..." npx playwright test brain` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>DB_PASS=$(awk '/### smaran-dev/{f=1} f && /db pass/{print $NF; exit}' docs/db-creds) && DATABASE_URL="postgresql://postgres.ajzoxgjvtxhzzkgxxmxv:${DB_PASS}@aws-1-ap-south-1.pooler.supabase.com:6543/postgres" INTERNAL_API_KEY="e2e-local-key" npx playwright test brain</automated>
  </verify>
  <done>Playwright E2E tests verified.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | Daily scan payload limits | mitigate | Segment database queries and dispatch notifications using throttled async promise queues |
</threat_model>

<verification>
- Playwright E2E specs pass.
</verification>

<success_criteria>
- Lapsed events are parsed.
- Cron endpoints trigger alerts successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/04-daily-brain-scheduling/04-03-SUMMARY.md`
</output>
