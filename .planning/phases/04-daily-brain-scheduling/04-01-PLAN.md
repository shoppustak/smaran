---
phase: 04-daily-brain-scheduling
plan: 01
type: execute
wave: 1
depends_on: ["03-03"]
files_modified:
  - code/artifacts/api-server/src/routes/cron.ts
  - code/artifacts/api-server/src/lib/brain.ts
autonomous: true
requirements: [BRAIN-01]

must_haves:
  truths:
    - "Daily cron triggers execute via POST /cron/daily-brain"
    - "Requests are gated securely via X-Cron-Secret header verification matching CRON_SECRET"
    - "Date resolution maps target Gregorian dates to Hindu calendar elements (maas, paksha, tithi) using Vedika"
    - "Lunar month names translate correctly based on the purohit's amanta/purnimanta preference setting"
    - "All database access in brain.ts is dynamically gated"
  artifacts:
    - path: code/artifacts/api-server/src/routes/cron.ts
      provides: "POST /cron/daily-brain route"
    - path: code/artifacts/api-server/src/lib/brain.ts
      provides: "resolveUpcomingEventsForWeek"
---

<objective>
Implement the daily cron route `/cron/daily-brain` gated by secret key validation, and build the month translation algorithm that resolves future dates to database events matching the purohit's calendar system choice.
This ensures upcoming rituals can be parsed and matched correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-daily-brain-scheduling/04-CONTEXT.md
@.planning/phases/04-daily-brain-scheduling/04-PATTERNS.md
@docs/smaran-blueprint-v3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Month Resolution Translation Algorithm</name>
  <files>code/artifacts/api-server/src/lib/brain.ts</files>
  <action>
    Create `code/artifacts/api-server/src/lib/brain.ts`.
    1. Define `resolvePurnimantaMaas(amantaMaas: string, paksha: "Shukla" | "Krishna"): string` translating month names for dark fortnight Krishna Paksha events under Purnimanta calendars.
    2. Implement `resolveUpcomingEventsForWeek(targetDate: Date): Promise<ResolvedBrainEvent[]>`:
       - Query the next 7 days of Gregorian dates starting at `targetDate` using the Vedika Panchang API (or sandbox proxy payload).
       - For each day, retrieve the Amanta-resolved `maas`, `paksha`, and `tithi`.
       - Query all `purohits`. For each purohit, retrieve their associated events.
       - If the purohit's calendar preference is `purnimanta`, translate the target maas name using `resolvePurnimantaMaas(maas, paksha)`.
       - Query `eventsTable` to find matching events (matching translated `maas`, `paksha`, and `tithi`).
       - Return a structured array containing matching events mapped to their Gregorian dates.
  </action>
  <acceptance_criteria>
    - `resolvePurnimantaMaas` matches months correctly.
    - Matches are filtered based on the calendar system settings.
    - Typecheck compiles cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Month translation matching logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Cron API Route</name>
  <files>code/artifacts/api-server/src/routes/cron.ts, code/artifacts/api-server/src/routes/index.ts</files>
  <action>
    Create `code/artifacts/api-server/src/routes/cron.ts`.
    1. Verify that `X-Cron-Secret` header matches `process.env.CRON_SECRET`. Return 401 on mismatch.
    2. Invoke `resolveUpcomingEventsForWeek(new Date())`.
    3. Iterate matched events and trigger notification dispatch.
    4. Respond 200 with JSON detailing matched items.
    Register the router in `code/artifacts/api-server/src/routes/index.ts`.
  </action>
  <acceptance_criteria>
    - Headers verified matching CRON_SECRET.
    - API returns 200 on successful match.
    - Router registered in index.ts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir code --filter @workspace/api-server run typecheck</automated>
  </verify>
  <done>Cron endpoint implemented.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | /cron/daily-brain route | mitigate | Gate endpoint execution with `X-Cron-Secret` validation check |
</threat_model>

<verification>
- Typechecking passes cleanly.
</verification>

<success_criteria>
- Cron endpoint triggers.
- Months resolve accurately.
</success_criteria>

<output>
After completion, create `.planning/phases/04-daily-brain-scheduling/04-01-SUMMARY.md`
</output>
